# 通过动作中介语言策略学习面向交互式智能体的以人为本交互策略

匿名作者

> 译文说明：本文严格以 `sigconf-authordraft.tex` 为主入口；按要求，图和表仅翻译 caption，图内文字与表格内容不作翻译。

## 摘要

交互式智能体正日益普及于日常生活，从语音助手、辅导系统到客户服务机器人。随着这一发展，对话已经成为它们理解人类需求并完成任务的主要渠道。然而，大多数智能体仍主要擅长狭义的问答，却难以完成使人类对话有效的、更丰富的交互工作，例如引导、追问、质疑、承接先前贡献以及跨轮次转移主动权。这些策略选择在端到端语言生成中仍是隐式的，因此难以从人类交互中学习、检查和评价。

我们提出语言—动作逆 Q 学习（Language-Action Inverse Q-Learning，LA-IQL），这是一种动作中介框架，能够从人类课堂轨迹中离线学习，无需在线探索或人工设计的标量奖励。LA-IQL 将理由生成、对人类可读交互动作用的选择和语言实现分离，使策略选择能够被显式检查。在留出 K–12 课程的长程 rollout 中，被评价的 TD 正则化 checkpoint 相比其 no-TD 对照表现出显著更少的重复行为，同时保持相近的局部人类模式对齐。与指标匹配的马尔可夫控制进一步表明，即使不读取对话也能取得很强的局部对齐；理由与语义干预分析则表明，学习到的策略捕捉了与动作有关且对上下文敏感的信息。直接选择更接近留出的人类动作模式，而受提案约束的重排序减少了重复，从而揭示出“对齐—重复”权衡。这些结果表明，动作中介提供了一个可检查接口，使人类模式对齐、时间组织、上下文敏感性和语言实现能够被分别分析。这有助于推动智能体交互超越表层流畅性，走向更自然、以人类交互为基础的互动。

**CCS 概念：** 以人为中心的计算—人机交互（HCI）；计算方法—强化学习。

**关键词：** 以人为本的交互、交互式智能体、动作中介策略、离线模仿学习、对话策略。

> **图 1：动作中介使交互策略显式且可检查。** 端到端生成把策略隐藏在文本中，监督模仿侧重局部复现，在线强化学习则依赖奖励设计与探索。LA-IQL 转而从真实人类轨迹及人类可读策略空间中进行离线学习，显式给出理由和所选策略，并以该选择为条件生成话语。这一动作边界使人类模式对齐、长程组织和策略—语言实现能够被分别观察。

## 1 引言

交互式智能体已经融入日常生活。语音助手回答问题，辅导系统引导学习者完成练习，客户服务机器人处理问题，陪伴型智能体则提供社会互动。在这些不同场景中，对话是智能体理解人类意图、协商意义和完成任务的基本接口 [park2023generative; xi2025rise]。这种对话式交互的质量，日益决定了系统给人的感受是有帮助、自然且引人投入，还是机械、僵化且令人不满。

然而，当前智能体的交谈方式与人类的自然互动之间仍存在持续的差距。多数当代对话智能体擅长回答问题和执行直接指令，却难以掌握有效人类交流所包含的更广泛交互行为。熟练的人类交谈者所做的远不只是回应：他们会提出澄清问题、追问不完整答案、质疑假设、提供引导、承接先前贡献，并认可和接续他人的发言。这些交互层面的动作不同于任一单独话语的命题内容，并决定对话是动态、具有响应性，还是平淡、重复。当智能体缺乏这些能力时，互动会退化为可预测的信息交换：智能体等待问题、给出答案，然后再次等待。这足以处理简单查询，却恰恰无法满足交互式智能体最具潜力的丰富、多轮、混合主动场景。

这一局限的根源在于架构。当前交互式智能体通常端到端地生成对话，使每个回应背后的交互策略隐含在生成文本中 [achiam2023gpt; kasneci2023chatgpt; park2023generative; xi2025rise]。一个局部合理的话语可能是在澄清、质疑、引导、重复先前模式或让出主动权，但这些策略选择从未被显式表示、评价或控制。即使每一轮单独看来都很流畅，累积起来仍可能形成重复或协调不良的交互 [li2016diversity; zhao2017learning]。行为克隆和监督微调提供了直接模仿已观察行为的方法，但主要优化局部动作或词元似然，并不显式建模交互选择如何随时间展开。在线强化学习能够优化序列行为，但当互动涉及真实人员或高风险环境时，其所需探索可能代价高昂或不安全。

这引出了一个核心问题：*交互式智能体如何从真实人类互动中学习更丰富、更接近人类的对话策略，同时使策略选择与语言实现保持足够分离，以便检查、评价和改进？* 我们认为，前进的关键是把交互策略变成显式、可学习且可检查的对象，而不是文本生成的隐性副产品。这意味着将“采取什么动作”（澄清、质疑、引导或认可）与“如何措辞”分开，使二者都能从人类示范中学习，并按照各自标准接受评价。

我们通过一种 human-grounded 形式化研究这一问题，其中动作库、训练轨迹和评价参照均来自已观察的人类交互。这里的 human-grounded 专指交互结构的来源：动作空间由成熟的认知动作标注体系定义 [songyu79]，训练则来自真实转录文本。它并不意味着生成对话在整体上像人、自然、符合上下文，或对人有效。

我们的核心贡献是一种动作中介范式：语言策略先选择一个离散且人类可读的交互动作，再用语言实现这一选择。这样便将策略来源——人类示范——与任一具体轮次的措辞分离，并形成可追踪的边界，使策略学习、时间组织、控制分配和最终语言实现能够作为彼此相关但不同的对象分别评价。从固定轨迹学习还避免了在线探索和人工设计的标量奖励 [li2016deep; ouyang2022training; schulman2017proximal]。

我们以课堂对话研究这一问题，它包含频繁且细粒度的交互动作。师生交流具有多角色和开放性，结合提问、承接、阐述、质疑、引导与综合，而不是遵循固定任务脚本。真实转录文本还配有成熟的认知动作标注体系，为交互策略提供了可观察的操作化方式 [songyu79]。我们的目标是研究策略丰富的人类交互如何为交互式智能体的对话策略奠定基础。

我们提出 **Language-Action Inverse Q-Learning（LA-IQL）**，这是一个由领域特定动作空间和人类示范配置的离线框架。LA-IQL 在固定转移上通过以理由为条件的离线逆软 Q 学习来学习动作轨迹 [garg2021iqlearn; wulfmeier2024imitating]，从学习到的策略中选择交互动作，并把语言实现交给角色模型。我们评价生成轨迹是否复现留出的人类动作模式、这种对齐是否伴随重复行为，以及理由是否携带与动作有关的信息。我们还评价动作控制分配与最终长程对话中可观察的知识引入、关联和发展之间的关系。在 250 张带证据的知识图谱上，Direct 比 Candidate 引入更少的唯一实体，同时保持几乎相同的平均关系度与后期关系增长。这体现出一种更紧凑而非一概更丰富的知识建构模式。

> **图 2：以人为本、动作中介的交互。** LA-IQL 从真实交互轨迹中学习人类可读的策略层，并将策略选择与语言实现分开。该边界既使行为模式可以追踪，也暴露出最终话语未能表达所选动作时的实现风险。

本文贡献有三点：

1. 一种用于 human-grounded 交互策略学习的动作中介形式化，将显式策略选择与语言实现分离，使交互式智能体不仅能够学习说什么，还能学习如何互动。
2. LA-IQL 将以理由为条件的动作预测与固定人类交互轨迹上的离线逆 Q 学习相结合，无需在线探索或手工奖励即可学习序列交互策略。
3. 一套用于考察动作模式对齐、重复性、上下文敏感性、理由—动作关联、语言实现和知识结构的长程评价协议，揭示局部相似性或流畅文本本身可能掩盖的行为差异。

通过使交互策略显式化，并使其能够从人类示范中学习，本研究向这样一种交互式智能体迈出了一步：它们不只是回答问题，而是能够参与使人类对话有效的丰富、适应性强且真正协作的交流。

## 2 相关工作

### 2.1 教学对话中的交互策略学习

端到端对话生成已经从序列到序列模型和分层模型发展到预训练 Transformer [sutskever2014sequence; serban2017hierarchical; vinyals2015neural]。角色提示和多智能体模拟如今能够生成持续的社会、任务和课堂交互 [park2023generative; xi2025rise; zhang2024simulating]，但流畅的角色话语本身并不能揭示或控制产生该话语的教学决策。因此，近期教育系统开始引入中间教学结构。BIPED 从真人辅导数据中归纳细粒度教师和学生对话动作，并先预测教师动作，再生成相应回应 [kwon2024biped]。StratL 用表示为转移图的预设多轮辅导计划来引导 LLM [puech2025steering]；ScaffoldLM 则维护分步计划、推断出的学习者状态和评估驱动记忆，以跨轮选择辅导动作 [li2026scaffoldlm]。这些系统共同体现了从无约束回应生成向显式教学规划与动作选择的转变。LA-IQL 同样分离决策与语言实现，但它从固定课堂轨迹中学习人类标注的认知交互动作策略，而不是预先规定辅导计划或推断特定任务的解题路径。

这种分离也把教育对话与对话策略学习联系起来。任务型对话通常被形式化为序列决策：根据对话状态选择抽象动作，并通过强化学习优化策略 [li2016deep; li2018dialogue]。在教育对话中，近期在线 RL 工作利用模拟的师生交互，并在教学支持与任务成功之间设置显式权衡，以对齐辅导模型 [dinucu2025teaching]。这类基于模拟的优化提供了可控目标，但依赖模拟器和奖励的有效性。离线方法则避免新增交互：CHAI 用学习得到的 Q 函数对语言模型候选项排序 [verma2022chai]，IQ-Learn 从专家示范恢复策略 [garg2021iqlearn]，Wulfmeier 等人 [wulfmeier2024imitating] 则把语言上的逆软 Q 学习表述为带时序差分正则项的 MLE。LA-IQL 将最后一种目标从词元级模仿适配为 31 类师生动作策略，即两个角色乘以 15 个认知动作符号，再加 `END`，并以真实课堂轨迹为基础 [songyu79; amatari2015]。本文并未提出新的 IRL 目标；其贡献是在无需在线探索或手工标量奖励的条件下，结合以理由为条件的动作选择、动作级 TD 正则化和独立语言实现。这使教学对话成为研究更广泛交互式智能体问题的可观察测试场景：智能体不仅要学习说什么，也要学习如何互动。

### 2.2 主动性、混合主动权与控制分配

HCI 研究认为，交互式智能体的能动性不只是回应质量：智能体必须决定何时以及如何参与、承担多少控制权，以及如何让人理解其行为。Inner Thoughts 框架将隐性的想法形成与显性的参与分离，以支持主动对话 [liu2025proactive]。共享自治研究发现了非单调权衡：委托智能体进行沟通可以帮助多任务处理，却也可能造成不适；共享控制又可能引入监控成本 [cheng2025surrogates]。在群体对话中，用户同样排斥主导谈话的智能体，并希望控制智能体何时、说什么以及在哪里参与 [houde2025controlling]。这些工作把主动权理解为控制分配问题，而不是认为更高自主性总是更好。本文的分配边界位于生成交互系统内部：我们比较从完整动作空间进行选择的学习策略，以及由角色模型先限制可用动作的提案约束重排序。该比较不测试用户控制，也不能确定更优的自主水平；它是在动作层让策略与角色模型之间的分工变得可检查。

### 2.3 教学对话中的交互行为评价

评价同样必须区分结构忠实度与教学质量。MRBench 对辅导回应的八个基于学习科学的维度提供人工标注 [maurya2025unifying]。ReWIRED 对专家解释性对话中的细粒度教学动作进行标注，并表明少样本 LLM 难以恢复这些动作，而且解释的功能质量需要超越静态结构指标的评价 [anagnostopoulou2025rewired]。EducationQ 则在动态多智能体场景中评价教学能力，并发现通用模型能力与教学效果并非单调对应 [shi2025educationq]。知识图谱表示可以在对话话语与有依据的信息项之间提供语义层 [schneider2024bridgekg]，但它本身不能确立教学价值或学习效果。

模拟学习者的有效性是另一个独立问题。教师报告称，提示产生的 LLM 学生可能表现得注意力异常集中、语言过度复杂并存在逻辑不一致 [martynova2025simulate]；2026 年更广泛的基准研究同样发现，简单提示无法充分满足语言、行为和认知层面的模拟标准 [scarlatos2026simulated]。因此，本文把课堂模拟作为交互式智能体策略评价的可观察测试场景，并将动作模式多样性、专家对齐、理由—动作关联以及可观察知识结构视为彼此独立的诊断。我们不把生成的课堂轨迹当作真实学习者行为、教学适切性或学习结果的证据。

## 3 方法

LA-IQL 将交互式智能体建模为序列决策者：它在每一步观察对话历史，选择领域定义的交互动作，再以自然语言实现该动作。真实人类轨迹为动作空间与监督提供基础，而显式动作边界使策略选择能够与语言实现分开检查。依照离线逆强化学习的模仿学习视角，LA-IQL 保留监督微调的可扩展性，同时在专家轨迹上加入感知动力学的正则项。本文的技术贡献是集成以理由为条件的策略状态、动作级 TD 正则化模仿、直接策略解码与独立语言实现，而不是提出新的 IRL 目标。我们还考察了受提案约束的候选重排序这一推理时调整。

### 3.1 问题形式化与交互动作空间

我们把交互式智能体问题实例化为有限上下文马尔可夫决策过程（MDP）近似，并从专家课堂示范中进行离线策略学习。令 $H_t$ 表示步骤 $t$ 之前的完整交互历史。策略观测 $s_t=f_{\mathrm{short}}(H_t)$ 由此前两轮对话和两个动作标签组成；它是近似马尔可夫状态，可能遗漏更长程的教学上下文。动作 $a_t$ 是下一待生成的交互动作，来自 31 个离散动作：$\{\text{学生},\text{教师}\}\times 15$ 个认知动作符号（例如基础知识提问/回应、分析与阐述、总结、迁移与创新、回应构建、赞同/质疑/引导），外加终止符 `END`。

被选中的话语 $u_t$ 及其动作将历史推进为 $H_{t+1}=H_t\oplus(a_t,u_t)$，之后 $s_{t+1}=f_{\mathrm{short}}(H_{t+1})$；`END` 标志情节终止。因此，部署时的转移由动作策略和语言实现模型共同诱导，而非仅由 $a_t$ 决定。给定专家示范 $\mathcal D_E=\{(s_t,a_t,s_{t+1})\}$，我们学习动作策略，并根据生成轨迹的多样性及其与专家动作模式的相似度进行评价；语言实现另行评价。这种动作级形式化是 LA-IQL 的第一项设计选择：不直接在开放式词元上应用逆 RL，而是在人类可读的师生认知动作上优化，把表层实现留给独立角色模型。

> **表 1：31 维交互动作空间的紧凑定义。**

我们评价交互模式多样性（G1）、专家模式对齐（G2）和理由—动作一致性（G3）。G4 通过在匹配长程轨迹上计算带证据的图端点，评价生成对话中可观察的知识建构模式。这些目标共同操作化交互式智能体框架所揭示的维度：对齐、时间组织、动作相关推理、语言实现与长程知识结构。

### 3.2 数据构建与理由标注

每条专家课堂轨迹被转换为固定窗口转移。在步骤 $t$，$s_t$ 包含最近两句话语和最近两个动作标签，$a_t$ 是已标注的下一交互动作，$s_{t+1}$ 则通过追加下一句话语及 $a_t$ 得到。标注模型依据局部状态和专家下一动作构造理由目标 $z_t^*$，但训练后的模型在推理时不会收到专家动作，而是学习如下分解：

$$
p_\theta(z_t,a_t\mid s_t)=p_\theta(z_t\mid s_t)\,\pi_\theta(a_t\mid s_t,z_t).
$$

训练前，我们过滤掉复制动作符号或规范标签的理由目标，以减少简单标签泄漏。固定监督格式为：

`<Rationale> 解释文本 </Rationale> <Action> 角色-符号标签 </Action>`。

这一构造把每个专家转移变成理由生成和动作级策略学习的配对监督，无需人工指定标量奖励。由于标注模型能看到专家动作，而部署后的理由生成器看不到，训练中混合教师强制理由和模型生成理由，以减小这种分布偏移。

### 3.3 从 MLE 到专家分布匹配

LA-IQL 在以理由为条件的双头模型监督微调基础上加入离线 IQLearn。MLE 在局部预测已观察动作，而 IQLearn 项无需在线交互或学习模拟器，便能把策略与连续状态价值耦合起来。

### 3.4 TD 正则化的离线 IQLearn 目标

我们没有提出新的逆强化学习目标，而采用 Wulfmeier 等人用于语言模仿的离线逆软 Q 学习形式 [wulfmeier2024imitating]。该形式以 IQ-Learn [garg2021iqlearn] 为基础，把逆 RL 重写成带 TD 正则项的最大似然训练。我们的贡献是把它从词元级生成适配到 31 类认知动作策略，其 Q 值由以理由为条件的策略头产生。

定义增广策略状态 $x_t=(s_t,z_t)$。对教师强制样例，$x_t^*=(s_t,z_t^*)$ 与 $x_{t+1}^*=(s_{t+1},z_{t+1}^*)$ 使用相应的已标注理由目标；模型生成理由的样例则以 $p_\theta(z\mid s)$ 的样本替代这些目标。推理时，语言头先根据 $s_t$ 生成 $z_t$，再进行动作评分。

目标包含四项。第一，$\mathcal L_{\mathrm{lm}}$ 是对 LLM 标注的理由和动作前缀计算的语言建模损失。第二，以动作级 MLE 监督策略头：

$$
\mathcal L_{\mathrm{policy}}=-\log\pi_\theta(a_t\mid x_t).
$$

第三，LM 头在理由之后给固定动作标签模板评分，从而产生软动作分布 $p_{\mathrm{LM}}(\cdot\mid x_t)$。使用 KL 一致性损失使策略头与该软分布对齐：

$$
\mathcal L_{\mathrm{consistency}}=D_{\mathrm{KL}}\!\left(p_{\mathrm{LM}}(\cdot\mid x_t)\,\|\,\pi_\theta(\cdot\mid x_t)\right).
$$

最后，策略头为每个 $a\in\mathcal A$ 产生 $Q_\theta(x_t,a)$。相应的软价值和策略为：

$$
\begin{aligned}
V_\theta(x_t)&=\log\!\sum_{a\in\mathcal A}\exp Q_\theta(x_t,a),\\
\log\pi_\theta(a\mid x_t)&=Q_\theta(x_t,a)-V_\theta(x_t).
\end{aligned}
$$

对专家转移 $(x_t,a_t,x_{t+1})$，动作级 TD 残差定义为：

$$
\delta_t:=V_\theta(x_t)+\log\pi_\theta(a_t\mid x_t)-\gamma V_\theta(x_{t+1}).
$$

当 $a_t=\texttt{END}$ 时令 $V_\theta(x_{t+1})=0$。由于 $V_\theta+\log\pi_\theta=Q_\theta$，对应的隐式奖励为 $\delta_t=Q_\theta(x_t,a_t)-\gamma V_\theta(x_{t+1})$。于是 $-\log\pi_\theta(a_t\mid x_t)+\lambda_4\delta_t^2$ 就是 Wulfmeier 等人推导的 MLE 加 TD 目标在动作级的对应形式。总损失为：

$$
\mathcal L=\mathcal L_{\mathrm{lm}}+\lambda_2\mathcal L_{\mathrm{policy}}+\lambda_3\mathcal L_{\mathrm{consistency}}+\lambda_4\delta_t^2.
$$

$\lambda_4$ 在 Stage 1--2 中均为 0；Stage 3 中 Full 将其设为 1，而 no-TD 保持为 0，关闭该项时即退化为无 TD 的目标。实际训练采用连续三个阶段，每个阶段均进行 2 个 epoch 的 LoRA 训练。Stage 1 强调结构合法性，学习率为 $10^{-4}$；Stage 2 从 Stage 1 final 继续联合优化语言与策略目标，学习率为 $10^{-4}$；Stage 3 从 Stage 2 final 继续联合优化，学习率为 $5\times10^{-5}$，并提高自生成理由的比例。三个阶段的教师/自生成理由比例依次为 1.0/0.0、0.8/0.2 和 0.6/0.4。基础模型为 `Qwen/Qwen3.5-0.8B`，最大长度为 512，实际物理 batch size 为 4、梯度累积步数为 1、有效 batch size 为 4，训练 seed 为 42，梯度裁剪为 1.0。Full 与 no-TD 的已恢复配置仅在 Stage 3 的 TD loss 权重上不同，分别为 1 和 0；配置中的“+ TD”旧名称不作为判断依据，以实际 loss 权重为准。

### 3.5 以理由为条件的策略参数化

模型采用显式的、以理由为条件的双头设计，这是 LA-IQL 的第三项设计选择。语言头（因果 LM）先生成 `<Rationale>` 片段，再生成 `<Action>` 标签；策略头对理由片段应用交叉注意力池化，并为每个动作输出一个价值，以参数化 $Q(x,a)$ 与 $\pi(a\mid x)$。决策以生成的理由为条件，而不是依赖单个池化表示。

具体而言，对输入词元序列：语言头产生 $\mathcal L_{\mathrm{lm}}$ 的词元级分数，并通过给 31 个动作的动作前缀模板评分得到软 LM 动作分布；策略头汇聚理由文本，输出用于 $\mathcal L_{\mathrm{policy}}$ 的策略分数；一致性损失 $\mathcal L_{\mathrm{consistency}}$ 则使 LM 动作前缀分布与策略 logits 对齐。

### 3.6 直接策略解码与语言实现

推理时，LA-IQL 将短上下文动作选择与长上下文语言实现分开。令 $h_t=f_{\mathrm{long}}(H_t)$ 表示角色模型看到的较长历史，它不同于策略观测 $s_t$。策略模型先生成 $z_t\sim p_\theta(z\mid s_t)$，再直接选择：

$$
\hat a_t=\arg\max_{a\in\mathcal A}Q_\theta(s_t,z_t,a).
$$

随后，角色模型生成一个实现 $u_t\sim q_\phi(u\mid h_t,\hat a_t)$。因此，LA-IQL 的默认推理规则让学习策略能够访问完整动作空间，而较长上下文只用于实现该策略所选的动作。

> **图 3：LA-IQL 框架与技术流程。** 左侧将以人为本的课堂对话和认知动作标注转换为理由增强的专家转移；中间的理由条件 LA-IQL 通过语言、一致性、策略和 TD 目标联合训练语言组件与策略/价值组件；推理时，学习策略选择交互动作，长上下文角色模型将其实现为话语。右侧还展示候选重排序这一推理时替代方案。

角色模型使用 30 轮对话和 5 个动作步骤的长窗口；需要采样时，温度为 0.8、top-$p$ 为 1。在报告的评价中，生成在遇到 `END` 或达到 100 动作上限时停止。

我们还探索了候选重排序。角色模型按固定格式采样六个动作—话语候选——教师和学生各三个——由此在 $\mathcal C_t=\{(a_{t,j},u_{t,j})\}_{j=1}^6$ 上诱导 $q_\phi(\mathcal C_t\mid h_t)$。选择规则为：

$$
\begin{aligned}
j^*&=\arg\max_{1\le j\le6}Q_\theta(s_t,z_t,a_{t,j}),\\
(a_t,u_t)&=(a_{t,j^*},u_{t,j^*}).
\end{aligned}
$$

该调整通过候选的动作标签评分，而不直接评价 $u_{t,j}$；因此，带有相同动作的候选对 $Q_\theta$ 而言不可区分，平局时保留提案顺序。它受到提案约束，因为未出现在 $\mathcal C_t$ 中的动作无法被选择。与默认直接解码不同，此时由角色模型控制动作支持集，价值模型只在提议动作中选择。

## 4 实验设计

### 4.1 研究问题

本文回答四个研究问题。它们共同检验交互式智能体的显式策略层是否与人类模式对齐、具有时间组织、对上下文敏感且能够清晰实现：

- **RQ1：** 动作中介的学习策略与直接语言生成及统计策略相比，在行为多样性、重复性以及与留出的真实人类交互模式对齐方面表现如何？
- **RQ2：** 在被评价的固定 checkpoint 中，含与不含 TD 正则化的条件之间，以及已完成配置下的直接全动作空间选择与受提案约束候选重排序之间，观察到哪些长程行为差异？
- **RQ3：** 在排除对话状态和显式标签泄漏后，生成理由是否仍包含与动作有关的信息？
- **RQ4：** 在学习策略与角色模型之间采用不同动作控制分配时，长程模拟课堂对话中会呈现哪些可观察的知识引入、关联和发展模式？

### 4.2 数据与划分

我们使用第 3.1 节所述 K–12 课堂语料中的 50 节课。每轮对话都有说话者角色和一个认知动作符号，由此构造 31 个交互动作及 LA-IQL 所用转移元组。采用冻结、分层的课级划分：35 节训练、5 节验证、10 节测试，而不是按对话轮划分，以避免同一课堂会话的窗口跨分区出现。划分按学科和学段分层，三个分区的课程标识互不重叠；预先冒烟测试所用的两节课不进入测试集。正式评价前记录划分清单、随机种子、数据哈希和各划分的动作频率。相同测试课程曾在较早的 20 步上限运行中被检查过；我们披露这一先前访问，并且不为本文报告的 100 步上限基线评价改变测试成员。

### 4.3 基线与消融

我们将 LA-IQL 与五种已完成的控制方法比较：直接角色条件生成、约束教师提示、专家频率采样、一阶马尔可夫策略和均匀动作采样。后三者隔离分布与序列统计，是诊断性控制，而非学习得到的状态条件策略。马尔可夫控制从冻结训练划分估计 $p(a_t\mid a_{t-1})$，其归纳偏置有意与基于二元组和转移的 G2 指标匹配，但它不观察对话内容、当前教学状态或理由，也不产生可检查实现的话语。它用于检验仅靠总体动作转移能恢复多少局部人类模式对齐。

LA-IQL 使用直接解码规则。我们还报告不重新训练策略、用候选重排序替代直接规则的配置。该比较是在查看当前留出结果后提升为重点，因此属于描述性分析，而非预注册的模型选择主张。两种配置使用相同检查点、课程、上限、角色模型温度和生成种子，但 Direct 的理由温度为 0.0，Candidate 为 0.6；因此我们把它们视为两种系统配置，不把差异完全归因于解码模式。

为考察与 TD 正则化相关的 checkpoint 层差异，我们加入 **LA-IQL w/o TD (Direct)**，其 Stage 3 TD 权重为 0，而不是 1。它使用 Direct 解码、0.0 理由温度以及相同的留出课程、角色模型、上下文窗口、生成种子、上限和停止规则。已恢复的其他训练字段也相同，但每个条件只有一个训练 checkpoint；因此本文把它作为固定-checkpoint 对比，而不是可推广到训练 seed 的因果估计。

### 4.4 评价指标

交互模式多样性（G1）报告动作 Distinct-2、两步循环率、经验动作熵和专家模式覆盖率，分别刻画局部多样性、重复退化、分布多样性及专家支持的广度。专家模式对齐（G2）采用生成动作模式与专家动作模式之间的 Jensen–Shannon 散度、转移矩阵距离及专家对生成动作 $n$ 元组的支持。这些是行为分布指标，并不能检验某动作对当前对话状态是否合适。马尔可夫控制的归纳偏置与这些指标重叠，因此它是严格的局部模式参照，而不是状态敏感交互能力的证据。

理由—动作一致性（G3）中，我们遮蔽确定性的标签字符串，并在互不重叠的课程上训练独立 31 类动作预测器，输入分别为状态、理由或二者。主要自动指标是增量可模拟性：

$$
\Delta_{\mathrm{sim}}=F_1^{\mathrm{macro}}(s_t,z_t\!\rightarrow\!a_t)-F_1^{\mathrm{macro}}(s_t\!\rightarrow\!a_t),
$$

其中目标是被评价系统选择的动作。我们还报告遮蔽后的仅理由 Macro-F1、多数类、泄漏及打乱理由控制。可预测性本身不能证明因果忠实性 [hase2020leakage; jacovi2020faithfully]，因此把 G3 称为可模拟性诊断，而不是解释质量指标。次级语言实现评价采用冗余和规则定义严重违规等保守、可复现的检查；这一有意收窄的诊断集合不衡量上下文或课堂适切性。G4 以“系统—生成种子—课程”为统计单位，主要证据是带逐字证据约束的知识图谱抽取，以及随后确定性计算的三个预指定端点：每 100 个非 `END` 话轮的唯一实体数、平均关系度，以及后半程首次引入的关系占最终关系的比例。规则诊断提供可复核锚点。本文报告知识图谱端点，但不把它们合成为单一“课堂质量分”。完整协议见附录 D。

### 4.5 评价协议

动作模式 JSD 是主要 G2 自动终点。当前轨迹结果覆盖生成种子 42–44，每个种子包含 10 节冻结测试课程，生成上限为 100；这些是来自每个固定 checkpoint 的三次随机 rollout，并非独立训练模型。报告生成种子聚合值的均值 $\pm$ 总体标准差。对学习系统的比较，还配对 30 个相同“种子—课程”轨迹，以 10 节课程进行聚类重采样，执行 10,000 次 bootstrap 并报告百分位 95% 区间。TD 的 $p$ 值在探索性 matched-checkpoint family 内未经校正；Direct–Candidate 表对保留的 10 项比较进行 Benjamini–Hochberg 校正。TD 对比保持 Direct 解码与理由温度固定；Direct 与 Candidate 则理由温度不同，且在观察留出结果后才被考察，因此仅是描述性的系统对比，而非验证性因果解码效应。

> **表 2：G1 核心结果。** 数值为三个种子的均值 $\pm$ 总体标准差。粗体表示每列中表现最好的学习系统；诊断基线可能具有多样性，却不一定与专家对齐。

> **表 3：G2 专家模式对齐。** 数值为三个种子的均值 $\pm$ 总体标准差。粗体表示在所示精度下数值最佳或实质并列的学习系统。一阶马尔可夫控制直接建模若干列所测的局部转移，但不以对话内容为条件。

### 4.6 实验结果

**数据与实现。** 语料含 50 节课、9,419 轮、47 个主题和 9,369 个转移。冻结的课级划分为 35/5/10。每个系统运行三个生成种子（42–44），每节测试课程最多生成 100 个动作。所有正式轨迹在第 50 个动作之前禁止 `END`。频率与马尔可夫控制只使用冻结训练集。主 Direct 和候选重排序使用 `sft_stage3/final` checkpoint；w/o-TD 固定-checkpoint 对比使用 `sft_stage3_woTD/final`。主 Direct 与 w/o-TD Direct 都使用 0.0 的理由温度，其他生成设置相同；Candidate 使用 0.6，因而不是单因素解码消融。30 条 w/o-TD 轨迹中有 5 条在 57–95 个动作后终止，而主 Direct 的 30 条均达到 100。该停止行为是共享规则下观察到的策略结果，并非事后截断。

**被评价的 TD checkpoint 呈现出更少重复的长程组织。** 匹配的 Direct 解码 checkpoint 对比显示，w/o-TD checkpoint 的两步循环率更高（0.2607 对 0.1708；TD 减 w/o-TD 为 $-0.0900$，95% CI $[-0.1400,-0.0438]$，$p=.0002$），模体复现率也更高（0.5379 对 0.3957；差值 $-0.1422$，95% CI $[-0.2013,-0.0889]$，$p=.0002$），相对差异分别为 53% 和 36%。使用 TD 时 Distinct-2 和归一化熵在数值上更高，但差异并不可靠。两个系统都覆盖了全部前 20 个专家模式，表明较低复现率并非通过放弃常见模式实现。

局部专家分布对齐变化很小。含 TD 与不含 TD 的配对二元组 JSD 分别为 0.0827 和 0.0835（差值 $-0.0007$，95% CI $[-0.0063,0.0043]$，$p=.8281$），转移 JSD 同样未检测到可靠差异。二元组支持率显示 TD 有边界性的描述优势（0.9906 对 0.9833，$p=.0510$）。对于这两个固定 checkpoint，观察到的 TD 相关差异主要集中在重复性序列结构，而非局部模仿保真度。因此，本文推断明确限定于被评价的 checkpoint，而不推广到随机初始化下的训练目标效应。

> **表 4：Direct 解码下的固定-checkpoint TD 对比。** 在相同 30 个“生成种子—课程”轨迹上比较含 TD 与不含 TD 的 LA-IQL；按 10 节留出课程聚类，执行 10,000 次 bootstrap。两个系统都使用 0.0 的理由温度及完全相同的其他生成设置。正差值表示 TD 模型数值更大，指标箭头表示期望方向。

**局部转移统计构成强但有限的参照。** 一阶马尔可夫控制取得最低的配对二元组 JSD（0.0791）、汇总二元组 JSD（0.0982）和转移行 JSD（0.1249），Direct LA-IQL 对应为 0.0827、0.1637 和 0.1299。它还达到 0.9935 的二元组支持率，而 Direct LA-IQL 的三元组支持率更高（0.8772 对 0.8557）。

这一结果来自模型与测量的实质匹配：控制方法从训练轨迹估计 $p(a_t\mid a_{t-1})$，二元组 JSD 和转移行 JSD 测量的恰是相应局部动作结构；保守重采样训练支持的转移也会带来高支持率和覆盖率。这些是该方法在既定 G2 终点上的真实优势，不能视为应被忽略的错误。但它从不观察对话文本或教学状态，无法说明局部常见转移在上下文中是否合适。因此，该结果证明简单序列统计可以复现局部人类模式，而不是证明马尔可夫策略是能力更强的交互智能体。这也说明为何必须将 G2 与状态条件决策轨迹、重复诊断及动作—话语实现检查一并报告。

**冻结干预为文本敏感性提供探索性证据。** 我们在 10 节留出课程中各选择 5 个预先存在且均匀间隔的状态，对主 Full checkpoint 进行评价（$N=50$）。在每节课内，循环选择的 donor 替换话语语义，同时保持说话者序列、话语数量和动作历史不变。相较原状态，模型分配给专家动作的概率下降 0.0767（按课程聚类的 bootstrap 95% CI $[0.0153,0.1288]$）；60% 的 argmax 动作发生翻转（CI $[0.48,0.72]$）；平均策略 JSD 为 0.2957（CI $[0.2476,0.3431]$）。31 类 Macro-F1 下降 0.0201，但区间跨越零（$[-0.0185,0.0649]$）。该干预表明，固定策略分布会对保留动作历史之外的对话文本作出响应；但样本较小、donor 构造具有确定性且缺少人类判断，因此不能证明这种响应在上下文中是适当的。完整协议见附录 C。

**在学习架构内部，对齐伴随着重复的局部结构。** Direct LA-IQL 比 Candidate 更接近留出专家分布：配对二元组 JSD 为 0.0827 对 0.1059，训练专家二元组支持率为 0.9906 对 0.9522。30 个匹配单元上的 Direct–Candidate JSD 差值为 $-0.0232$，95% CI $[-0.0305,-0.0152]$，BH 校正后 $q<.0001$。

但这种对齐不意味着交互动力学全面更好。Direct 的两步循环更多（0.1708 对 0.0488；差值 $+0.1220$，95% CI $[0.0986,0.1447]$，$q<.0001$），重复模体也更多（0.3957 对 0.1413；差值 $+0.2543$，95% CI $[0.2100,0.2963]$，$q<.0001$）。Distinct-2 没有可靠差异（$-0.0108$，95% CI $[-0.0377,0.0165]$，$q=.5483$）。因此，两种配置在动作序列组织上的差异比在局部组合多样性上的差异更清晰。

> **表 5：Direct–Candidate 配对比较。** 覆盖 30 个匹配“种子—课程”单元（10,000 次 bootstrap、百分位区间、对保留的 10 项比较作 BH 校正）。正值表示 Direct 数值更大，箭头表示期望方向。两种配置的理由温度也不同，因此这是系统级对比，而非单因素解码消融。

**规则选出的轨迹使重复差异可被检查。** 在阅读轨迹文本之前，我们先固定选例规则：选择 Direct–Candidate 两步循环率差值最大的匹配“课程—种子”单元；如有并列，依次按模体率差值和字典序破局。该规则选中生成种子 43 的初三化学“原子的构成”课程（两步循环率：0.309 对 0.052；模体率：0.55 对 0.17）。下表展示 Direct 最早出现的四回合循环，以及匹配 Candidate 轨迹中相同回合位置。动作符号沿用正文动作空间表。

> **表 6：正式轨迹中的规则选定案例（课程 4873，生成种子 43，第 13–16 回合）。**
>
> **Direct；局部上下文：**学生刚刚复述了教师对核力的解释。
>
> - **t13 教师–Ag：**理由为确认理解并推进问题；话语为“完全正确。质子越多是否意味着核力越强、原子核越稳定？”
> - **t14 学生–Pk：**理由为依据已有知识回答假设；话语为“我觉得是的，质子越多，核力应该越强。”
> - **t15 教师–Ag：**理由为肯定类比并引入竞争作用力；话语为“但静电斥力也会增大，这两种力最终谁占上风？”
> - **t16 学生–Pk：**理由为回应作用力比较；话语为“斥力也会增大，所以不一定会更稳定。”
>
> **Candidate；局部上下文：**学生刚刚估计了电子质量，并指出原子的大部分质量集中在原子核。
>
> - **t13 教师–Gu：**理由为确认估计并巩固对质量分布的理解；话语为“画出原子结构示意图，并标出各部分位置和大致质量占比。”
> - **t14 学生–An：**理由为构建并解释所要求的模型；话语为“中央的小原子核包含质子和中子，质量很轻的电子在核外运动。”
> - **t15 教师–Iu：**理由为细化图示中的组分位置和质量类比；话语为“什么作用力使电子和原子核维持稳定关系？”
> - **t16 学生–Pk：**理由为给出所要求的关系；话语为“带负电的电子与带正电的原子核之间的吸引力。”
>
> Direct 窗口构成 Ag–Pk–Ag–Pk 两步循环，Candidate 则依次为 Gu–An–Iu–Pk。表中保留了记录的动作、理由意图和话语内容，并仅为篇幅压缩措辞。

该动作轨迹暴露了仅从表层文本难以审计的重复决策类型；与此同时，Direct 的话语内容仍从核力判断推进到了静电斥力的制衡作用。Candidate 的第 15 回合还暴露出局部理由—实现偏移：记录的理由聚焦于细化位置和质量关系，最终话语却转向静电稳定性。因此，这一案例只能解释汇总重复差异并提示实现风险，不能单独验证重复或实现质量的教育含义：重复动作模体不等于逐字重复，也不自动等于教学失败。本文据此把该案例用于结构解释，而不用于给出人类偏好排名。

**理由携带可预测动作的信息。** 在同一 G3-v2 双 TF–IDF 评价器下，Direct 和 Candidate 遮蔽后的仅理由 Macro-F1 分别为 0.3986 与 0.1013，状态单独输入则分别为 0.0159 与 0.0123。生成种子级增量可模拟性 $\Delta_{\mathrm{sim}}$ 分别为 0.3885 与 0.0863。w/o-TD Direct checkpoint 得到仅理由 F1 0.3682、组合 F1 0.3629、$\Delta_{\mathrm{sim}}=0.3372$、准确率 0.6867，描述性地低于 TD Direct 的 0.3986、0.4044、0.3885 和 0.7097。对原始 Direct 和 Candidate，观察到的生成种子级增量超过全部 20 个课内/角色打乱控制，30 个“生成种子—课程”增量也全为正。这说明遮蔽后的理由包含与所选动作相关的信息，但不能证明理由因果地决定策略决策或构成忠实解释；我们也不对描述性的 G3 TD 差异提出推断性效应主张。

**规则诊断界定了语言实现风险。** 最终的语言实现证据由保守、可复现的规则检查构成。Direct、Candidate 和 w/o-TD Direct 均无完全重复，也未触发规则定义的严重违规；Direct 与 Candidate 之间以及两个 Direct checkpoint 之间的局部三元冗余均无可靠差异。这些窄范围检查不衡量语言实现质量或课堂适切性。

**RQ4 显示出更紧凑但连接程度相近的知识建构。** 250 张抽取图谱全部通过了机器可检查的 schema 与话轮 ID 证据约束。下表报告三个预指定端点；对于生成系统，先在每个“系统—课程”单元内平均三个生成种子。

> **表 8：RQ4 知识图谱端点。** UER$_{100}$ 表示每 100 个非 `END` 话轮中带证据的唯一实体数。生成系统的数值先在 10 节留出课程内分别平均三个生成种子；专家行每节课程只有一条真实转录。这些端点描述生成文本中的知识结构，而不衡量事实正确性或教学质量。
>
> | 系统 | UER$_{100}$ | 平均关系度 | 后期关系增长 |
> |---|---:|---:|---:|
> | 专家转录 | 14.17 | 1.906 | 0.456 |
> | LA-IQL Direct | 23.95 | 2.112 | 0.379 |
> | LA-IQL Candidate | 31.14 | 2.116 | 0.379 |
> | LA-IQL w/o TD（Direct） | 27.96 | 1.963 | 0.380 |
> | Direct-role LLM | 38.65 | 2.159 | 0.334 |
> | 受约束教师提示 | 39.15 | 2.164 | 0.360 |
> | Frozen BC | 30.36 | 2.012 | 0.301 |
> | LoRA policy | 32.22 | 2.064 | 0.325 |
> | Likelihood rerank | 28.32 | 2.202 | 0.335 |

相较 Candidate，Direct 每 100 个话轮少引入 7.19 个唯一实体（95% CI $[-10.43,-4.25]$，Holm 校正后 $p=.0059$），而平均关系度（$-0.003$，CI $[-0.098,0.093]$）与后期关系增长（$-0.001$，CI $[-0.065,0.052]$）几乎不变。相较匹配的 w/o-TD Direct checkpoint，Direct 的 UER$_{100}$ 低 4.00（CI $[-8.69,-0.05]$，校正后 $p=.2109$），平均关系度高 0.149（CI $[0.059,0.240]$，校正后 $p=.0527$），后期关系增长则没有差异（$-0.001$，CI $[-0.048,0.050]$）。因此，RQ4 最清晰的模式并不是知识量一概更多：Direct 比 Candidate 形成更紧凑的图，同时保持相近的连接程度和关系发展时序。UER$_{100}$ 本身没有“越高越好”的方向，不能据此给出教学质量排名。

## 5 讨论

### 5.1 人类基础作为可检查的设计资源

我们的第一项贡献是方法论上的：动作中介把人类基础转化为一种可操作、可检查的设计资源，以构建更自然的交互式智能体。动作词汇、训练示范和留出比较模式均来自真实课堂互动，并通过共享的人类可读策略层连接策略学习、生成与评价。这超越了面向下一话语生成的结构化提示：它建立了一个稳定接口，使设计者能够检查选择了什么策略、策略如何随时间展开，以及每个选择如何在语言中实现。对于希望让智能体从单纯回答问题转向引导、追问与协作的 HCI 从业者，这种可追踪性至关重要，因为它区分了“期待模型已经习得合理的交互习惯”与“验证模型实际上学到了什么”。

与指标匹配的马尔可夫控制阐明了人类模式对齐能够和不能确立什么。它的强局部对齐说明，不读取对话也可以复现转移相似度；相较之下，语义干预表明，学习策略的分布会响应保留动作历史之外的对话内容。因此，人类模式相似性只有置于更广泛、可检查的评价中才具有信息价值，不能被当作上下文敏感交互的充分度量。这一发现也提醒更广泛的“从人类示范中学习”研究：匹配人类行为的表面统计，并不保证智能体已经内化了底层交互能力。人类基础仍不能确保行为整体上像人或符合上下文，但动作边界使相关成功与失败能够在实证上被区分，因而可以被有针对性地处理。

### 5.2 TD 相关差异揭示长程组织

匹配的 Direct 解码 checkpoint 对比隔离出最清晰的 TD 相关差异。w/o-TD checkpoint 的局部专家模式对齐未检测到可靠差异，但两步循环多 53%，重复模体多 36%。这一模式与 TD 项影响重复性序列组织、而非仅扩充动作词汇或改善单步模仿的解释一致；不过，每个条件只有一个 checkpoint，因此不能得出训练目标层面的因果结论。w/o-TD 策略还有 5/30 条轨迹在达到 100 动作前终止，而 TD 策略没有，为 rollout 行为分化提供了进一步描述性证据。由于每个条件只有一个训练 checkpoint，bootstrap 捕捉的是留出课程与生成随机性的变化，而不是训练 seed 不确定性。

这一结果展示了动作中介形式化在设计低重复、更具吸引力的交互式智能体方面的分析能力。它把观察到的 TD 相关差异定位在时间组织上——被评价的 TD checkpoint 两步循环少 35%，重复模体少 26%——而不是局部专家模式对齐或动作词汇扩张。这一区分直接关系到用户体验：一个智能体如果反复循环于少量交互动作，即使每个动作局部上都合适，在持续使用中仍会显得单调且可预测。端到端文本评价会掩盖这一区分，因为表层词汇变化可能遮蔽底层交互僵化。本文不把这一结构结果扩展为参与者感知的连贯性或有用性；后者不属于本文的实证范围，也是理解这些结构属性如何转化为用户体验的重要未来工作。

### 5.3 动作中介使控制分配可观察

把策略选择与语言实现分开，会把控制分配转化为可观察的设计变量。已完成的 Direct 配置允许学习策略访问完整动作空间，与留出人类动作模式对齐更强，但也产生更多短循环和重复模体。Candidate 配置把动作支持限制在角色模型提案中，重复更少，但其轨迹偏离留出动作分布更远。由于两种配置的理由温度也不同，这构成描述性系统级对比。尽管如此，已完成配置说明，更大的动作支持控制权不必然带来更低的重复率；策略支持、时间组织和措辞必须联合考虑。

这一发现对交互式智能体的配置具有实践意义。对齐与重复之间的权衡——即忠实复现人类动作分布与避免陷入对话惯性之间的权衡——不能仅靠赋予策略更多或更少控制权来解决，而需要仔细考虑各种约束应当作用于生成流水线的哪个位置。显式动作边界为这种结构可追踪性提供了具体载体，使系统设计者能够比较某一状态下选择的策略与最终生成的话语、检查反复出现的控制模式，并定位偏差源于策略选择还是语言实现。本文把它确立为一种方法层面的可追踪能力。对于构建交互系统的 HCI 研究者与从业者，这种可检查性也是负责任地迭代和改进系统的前提。

### 5.4 对交互式对话智能体评价的启示

当前测试平台对评价旨在进行更丰富、更接近人类对话的交互式智能体带来三点启示。第一，动作模式指标应配合归纳偏置与指标相匹配的控制方法，否则局部统计复现可能被误判为上下文敏感能力。一个智能体即使复现了人类交互动作的局部分布，也可能没有对实际话语作出有意义的回应；只有干预对话内容时，这类失败才会显现。第二，评价应把策略选择与语言实现视为彼此耦合但不同的对象：如果话语没有表达动作，再合适的动作标签也作用有限。智能体是否意图引导、质疑或认可，以及它是否确实通过语言完成了这些动作，是需要实证检验的问题，而不是先验事实。第三，长程重复必须与多样性和对齐同时报告，因为策略可能覆盖人类支持的模式，却反复循环于其中一个很小的子集。对于持续使用的交互式智能体，即使单个回应看似合适，这类失败也会侵蚀用户信任与投入。

综合来看，这些发现提出了一套面向策略丰富交互式智能体的评价框架：暴露人类可读的策略层，检验策略是否使用交互上下文，并把人类模式对齐、时间组织和语言实现作为互补而非可互换的属性来评价。这一框架直接回应了 HCI 社群对设计“不仅有能力，而且在真实人类互动中真正可用且引人投入”的系统的关注。动作中介方法提供了必要透明度，使评价能够从“智能体是否生成流畅回应”转向“智能体是否有效互动，以及这种互动的依据是否可观察”。

## 6 结论

交互式智能体日益嵌入日常生活，而对话仍是它们服务人类需求的主要渠道。然而，回答好问题与进行真正协作、适应性的互动之间仍存在显著差距。本文提出了一种用于学习 human-grounded 交互策略的动作中介形式化，并以 LA-IQL 将其实现。LA-IQL 从固定的人类交互轨迹中，通过以理由为条件的动作预测和离线逆 Q 学习，学习人类可读交互动作上的显式策略，同时将策略选择与语言实现分离。这种形式化使交互决策、时间组织和表层实现能够被独立检查；这是推动智能体超越机械响应、走向自然且对上下文敏感的互动的必要条件。

在留出课堂的长程 rollout 中，被评价的 TD 正则化 checkpoint 在保持相近局部人类模式对齐的同时，表现出更少的重复行为。改变策略与角色模型之间的控制分配还揭示出清晰的权衡：直接选择能更好地保留留出人类模式，而受提案约束的重排序减少了重复。综合而言，这些发现说明，动作中介能够揭示流畅文本或局部相似性本身可能掩盖的重要交互组织差异。

本研究更广泛的意义，在于把人类交互视为一种可学习、可检查的资源。通过使策略显式化，我们使设计者和研究者能够提出更精确的问题：交互式智能体究竟从人类示范中学到了什么、它们如何随时间组织行为，以及其局限位于何处。现有证据支持把动作中介作为面向策略丰富交互式智能体的可检查建模与评价接口；证据范围限定于被评价的 checkpoint 和结构行为。这些边界也界定了未来工作的方向：把动作中介学习扩展到更多交互领域，根据用户体验验证结构属性，并最终构建不只是回应人类、而是能够与人互动的交互式智能体。

## 局限性

LA-IQL 依赖源自一种课堂标注方案的固定交互动作分类。31 动作空间可能遗漏有教学意义的行为，也可能无法原样迁移到不同学科、年级、语言、文化或课堂形式。尽管框架围绕领域特定状态与动作空间设计，并由更广泛的交互学习问题驱动，本文只评价课堂对话实例，不对其向其他交互式智能体领域（包括对话助手、多智能体系统或具身智能体）的迁移作实证主张。方法从离线专家示范学习，因此会复现源语料的标注错误和系统偏差，也无法发现语料中不存在的有效策略。有限上下文策略观测可能遗漏长程教学目标，尽管角色模型能接收更长对话历史。

已完成的 Direct 与 Candidate 配置除动作选择机制外，理由采样温度也不同。因此，其比较识别的是可复现的系统级权衡，而不隔离解码模式的因果效应；解码机制层面的因果归因不属于本文主张。

生成理由不应被解释为模型内部计算的忠实因果说明。它们是受监督的文本中间变量，可能看似合理却不完整，或与所选动作不一致。受提案约束的调整受候选覆盖限制：再强的 Q 函数也无法选择提案集中不存在的动作，也不能区分带有相同动作标签的话语。直接解码避开支持集限制，却把所选动作的实现交给独立角色模型，后者可能无法忠实表达动作。

最后，自动动作分布指标不能证明教育效果，本文也不对学习成效或教师培训价值作出主张。G4 知识图谱端点同样只测量生成文本中表达的知识结构，而不是学生认知变化、事实正确性、教学质量或学习成效。机器可检查的 schema 与话轮引用本身不能证明所抽取实体和关系在语义上正确，重复自动抽取时精确边身份也可能变化。G2 终点偏重局部动作 $n$ 元组和转移分布；一阶马尔可夫策略可能仅通过保守复用训练支持的转移取得高分，而不响应当前课堂状态的语义内容。50 状态干预表明 Full 策略分布会随话语语义变化，但其确定性 donor 与样本规模把结论限定为分布敏感性，而非上下文决策质量。

## 伦理考虑

课堂对话可能包含个人信息并涉及未成年人，因此隐私、同意和数据治理是本研究的核心。任何课堂转录文本的使用都应遵循源数据集的同意条件、许可与访问限制，并在模型训练或发布前移除身份信息。生成对话不得被宣传为合格教师的替代品，也不得在缺少人类监督时用于高风险评价。

基于该框架构建的交互式智能体可能复现示范和底层语言模型中的文化、语言或教学偏差。尤其是，在狭窄课堂人群上训练的策略可能错误地把一种话语风格视为普遍的专家风格。因此，部署应包括子群体与跨情境评价、教育工作者审查、机器生成结果的清晰披露，以及让用户质疑或覆盖生成建议的机制。LLM 生成的理由标注也需要质量审计，因为流畅解释可能掩盖标注错误或无依据假设。

## 参考文献

参考文献的作者、题名及出版信息不作翻译，沿用源文件 [`references.bib`](references.bib) 中的原文书目。

## 附录 A：补充实验结果

> **表 A1：冻结课级划分的语料统计。** 各行主题数为去重计数，不一定相加等于语料总数，因为有一个主题名称出现在多个分区中。

**训练与推理配置。** LA-IQL 直接选择价值最高的动作，再取得一个角色模型实现。候选重排序则生成六个动作—话语提案，并把策略限制在该集合所含动作上。这改变了策略与角色模型间的控制分配，但不重新训练策略。已完成配置的理由温度也不同（0.0 对 0.6），所以下述比较是描述性系统对比，而非隔离的解码消融。w/o-TD Direct 条件仅从训练中移除 TD 项，保留主 Direct 的生成配置，包括 0.0 理由温度。其 30 条轨迹有 5 条在允许的最小上限之后终止，而主 Direct 没有。

> **表 A2：相同留出课程上的训练与推理比较。** JSD 是 G2 配对课级二元组散度。

图 A1 与 A2 从互补角度汇总这些比较：前者强调配对不确定性，后者保留报告的指标尺度和种子级变化。

> **图 A1：固定-checkpoint TD 对比与 Direct–Candidate 系统对比在统一期望方向后的配对效应。** 对结果表中标记 $\downarrow$ 的指标，将差值和区间乘以 $-1$，使正值始终有利于首先命名的系统（TD 或 Direct）。水平线为按 10 节留出课程聚类、执行 10,000 次配对 bootstrap 后的百分位 95% 区间。实心标记表示报告的 $p<.05$ 或 BH 校正 $q<.05$，空心标记表示不显著或未检验。由于两种配置的理由温度也不同，Direct–Candidate 面板是描述性系统对比。

> **图 A2：三个学习系统配置的 G1 与 G2 核心结果。** 点表示均值，误差线表示种子 42–44 的总体标准差，每个种子有 10 节留出课程。连接线仅为视觉引导，不代表时间轨迹；箭头表示期望的指标方向。

**G3-v2 理由—动作可模拟性。** 独立预测器遮蔽动作符号、规范名称、缩写及确定性标签字符串。状态与理由采用独立的字符 2–5 元 TF–IDF 表示和经验证集选择的线性分类器。三个系统使用相同的冻结评价器协议。

> **表 A3：G3-v2 理由—动作可模拟性。** 数值为三个种子级分数的均值。目标是各系统所选动作；该结果是关联诊断，而非因果忠实性度量。

**知识图谱评价覆盖。** 正式 G4 抽取包含九个系统的 250 张有效图谱，七组预指定 Direct 比较均完成按课程聚类的匹配统计。论文证据限定为表 8 的三个图端点，不构建综合课堂质量分数。

**完整 G1 多样性与重复诊断。** 下表报告全部已完成系统的完整 G1 指标。Cov.@20 表示：按课程支持度排序、并以出现次数破局的前 20 个专家二元或三元动作模式中，在生成轨迹里实际出现的比例。这些诊断必须联合解释；高多样性或低重复本身不代表更强的专家模式对齐。

> **表 A4：全部已完成系统的完整 G1 多样性与重复诊断。** 数值为生成种子 42、43、44 的均值 $\pm$ 总体标准差；每个种子包含 10 节留出课程。学习系统在这些种子间对每个条件复用同一个 checkpoint。诊断基线用于说明，高多样性或低重复本身不能建立专家模式对齐。

## 附录 B：实现与可复现性

**归档训练配置与来源边界。** 推理设置取自正式轨迹 JSON 文件末尾的 `config` 对象，数据来源信息取自冻结划分清单，模型身份取自 checkpoint metadata；这些产物是报告运行的权威依据。归档运行配置确认：连续进行三个 LoRA 阶段，每阶段 2 个 epoch；采用下述学习率与损失权重；物理 batch size 为 4、梯度累积步数为 1、最大长度为 512、梯度裁剪为 1.0，训练 seed 为 42。Full 与 no-TD 共用这些配置，仅 Stage 3 的 TD loss 权重不同，分别为 1 和 0。optimizer 细节、学习率 scheduler 与 warm-up、LoRA rank/alpha/dropout 和 target modules、数值精度、精确样本数与 optimizer step 数、模型/tokenizer revision、checkpoint selection rule 及历史代码版本未保存在归档产物中，因此本文不作报告。本文不用当前默认值替代这些字段，也不把结果描述为跨训练 seed 的重复验证。

> **表 B1：已恢复的 Full 与 no-TD 三阶段训练配置。** “Cons.” 表示跨头一致性损失权重，“LM-act” 表示 LM 动作分类损失权重。在已恢复字段中，两种配置仅 Stage 3 的 TD loss 权重不同。

Stage 1--3 的 recovery loss 权重依次为 0.5、0.5、0.75；action-token 权重为 8、5、5；structure-token 权重为 10、8、8；rationale-end-token 权重为 50、30、30；action-boundary-token 权重为 30、20、20。TD 折扣因子为 0.99。训练期间理由生成器最多生成 256 个新 token，温度为 0。配置注释中“micro-batch 1、累积 4”的描述已经过时；实际运行参数为物理 batch 4、梯度累积 1。

> **表 B2：系统来源矩阵。** “gen.” 表示对固定 checkpoint 使用的生成 seed；“train” 表示独立训练的 BC checkpoint。BC 模型已完成诊断，但未纳入正式 horizon-100 轨迹表。

<!-- 扩展来源表保留在源文件中，不纳入渲染后的附录。机器可读清单保留完整 SHA-256；外部 API 记录只保存可变别名，没有不可变的提供方模型 revision。 -->

**已完成的 BC 训练 metadata。** 冻结骨干的 BC checkpoint 使用 `Qwen/Qwen3.5-0.8B`、6,181 个训练样本、下一动作交叉熵目标、3 个 epoch、batch size 4、学习率 $2\times10^{-5}$ 和最大长度 512。LoRA BC checkpoint 使用相同骨干、目标和训练集，并加入 999 个验证样本；其配置为 3 个 epoch、batch size 32、学习率 $10^{-4}$、最大长度 256、rank 8、bfloat16 和 1 个梯度累积步。两类模型均保留独立训练 seed 42--44 及逐 epoch metadata。

**推理配置与质量门控。** 所有已报告的 Full、Candidate 和 no-TD 正式轨迹均使用两轮对话、两个动作的策略窗口；30 轮对话、5 个动作的角色窗口；策略最大长度 512；理由预算 256 个新 token；horizon 100；角色温度 0.8；候选重排序时每个角色生成 3 个候选；并在第 49 步结束前抑制 `END`，logit margin 为 3.0。Full 和 no-TD 的生成门控各包含 50 个样本，均报告 100% 结构合法、理由/动作标签闭合、无退化且未触及长度上限。这些检查证明的是 schema 合法性，而不是策略或理由质量。

**软件环境。** 锁定环境要求 Python 3.10 或更高版本，并解析到 PyTorch 2.11.0+cu128、Transformers 5.14.1、PEFT 0.20.0、scikit-learn 1.7.2、SciPy 1.15.3、OpenAI 2.52.0 和 Google GenAI 2.16.0。依赖 lockfile 与 CUDA 12.8 wheel 索引均包含在代码产物中。

## 附录 C：上下文敏感性干预

冻结的 `context-sensitivity-v1` 协议从冻结 G4 候选池中，为每节课选择 5 个预先存在且均匀间隔的留出状态。对每个状态，协议在同一课程内循环选择语义 donor，同时保持说话者数量与顺序以及完整动作历史不变。理由采用温度 0 的贪心解码，预算为 160 个 token；由于这是状态级测试，不应用 `END` 抑制。区间按课程聚类，使用随机种子 20260822 执行 10,000 次百分位 bootstrap。

> **表 C1：主 Full checkpoint 的探索性上下文干预（$N=50$ 个状态、10 节课）。** 主文报告按课程聚类的区间。该干预测试策略分布的敏感性，而不是上下文选择的正确性。

## 附录 D：评价细节

完整 G1 套件还包括 Distinct-1/3、重复局部模体和跨轨迹方差。G3 在预测前遮蔽动作符号、规范名称、缩写和确定性标签字符串，并报告泄漏率、逐类分数、混淆矩阵、多数类和打乱理由控制。

G3 评价通过独立预测和课内/角色打乱控制提供关联证据，其证据范围是可模拟性。本文评价不包括删除、释义或角色兼容反事实等因果干预 [deyoung2020eraser]，也不包括动作支持或课堂适切性的人类判断。因此，我们不对解释忠实性、教育效果或体验自然度作出主张。
