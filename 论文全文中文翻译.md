# 通过动作中介的语言策略学习以人为本的交互策略

匿名作者

> 译文说明：本文严格以 `sigconf-authordraft.tex` 为主入口；按要求，图和表仅翻译 caption，图内文字与表格内容不作翻译。

## 摘要

人类交互通过澄清、质疑、引导以及承接先前贡献等策略展开。语言模型能够生成流畅的对话轮次，但组织其长程交互的行为选择仍是隐式的，也未必反映人类实践中的模式。我们探讨如何以真实的人类交互为基础构建策略丰富的语言智能体，同时让策略选择保持足够显式，以便评价。我们提出语言—动作逆 Q 学习（Language-Action Inverse Q-Learning，LA-IQL）：一种动作中介的语言策略，将开放式行为分解为可读的理由、基于价值函数对人类可读交互动作的选择，以及独立的语言实现。LA-IQL 从人类示范中进行离线学习，不需要在线探索，也不需要人工设计的标量奖励。

我们在带有认知标注的 K–12 课堂话语上实例化该方法，使用 31 种师生动作，并在一组匹配训练消融和两种推理配置下，对 90 条最长 100 轮的正式轨迹进行评价。从直接解码中移除时序差分（TD）正则化，会使两步循环率从 0.1708 增至 0.2607、重复模体率从 0.3957 增至 0.5379，而配对二元组 JSD 没有可靠变化（0.0827 对 0.0835）。与受提案约束的候选重排序相比，直接动作选择更接近留出的人类动作模式（配对二元组 JSD 为 0.0827 对 0.1059），但产生了多得多的两步循环和重复模体。

一阶马尔可夫控制在若干局部对齐指标上表现最强，这说明总体的人类模式相似度本身不能证明策略能够感知上下文。这些结果表明，动作中介揭示了流畅话语或总体相似度指标可能掩盖的“对齐—重复”权衡。

**CCS 概念：** 以人为中心的计算—人机交互（HCI）；计算方法—强化学习。

**关键词：** 以人为本的交互、语言中介交互、对话智能体、动作中介策略、离线模仿学习、对话策略。

> **图 1：从人类实践到可检查的语言中介行为。** 真实对话以可读的交互动作为基础形成轨迹；LA-IQL 在语言实现之前显式给出理由和所选策略；由此形成的动作边界支持对行为选择与表层表达分别评价。在课堂测试平台中，现有结果揭示了人类模式对齐与重复之间的权衡；知识建构结果将在修订后的 G4 中另行评价。

## 1 引言

人类交互的组织方式并不只是由局部看似合理的话语构成。在持续的交流中，人们会提问、澄清、质疑、引导、承接先前贡献并转移主动权，从而形成可辨认的交互策略。语言正日益成为交互式助手、多智能体系统和具身智能体与人类及彼此协调的媒介 [park2023generative; xi2025rise]。对这些智能体而言，措辞流畅固然重要，但仅凭流畅性并不能证明交互在行为上具有多样性、响应性，或植根于人类实践模式。

大语言模型（LLM）通常依据主题、角色或任务元数据生成对话，其中也包括基于角色的多智能体模拟 [achiam2023gpt; kasneci2023chatgpt; park2023generative; zhang2024simulating; xi2025rise]。然而，其行为选择隐藏在开放式文本之中。一个看似合理的下一句可能是在澄清、质疑、引导、重复熟悉模式或让出主动权；即使每一轮局部都很流畅，累积起来仍可能形成重复交互 [li2016diversity; zhao2017learning]。行为克隆和监督微调能够直接模仿已观察样例，但它们优化的是局部动作或词元似然，并不显式建模更长程的交互动力学。在线强化学习可以优化序列行为，但与人或高风险模拟器进行探索可能代价高昂或不安全。

这一缺口引出了我们的核心问题：*语言智能体如何从真实对话中学习以人为本的交互策略，同时让这些策略足够显式，以便评价和控制？* 本文对“以人为本”作有意收窄的界定：动作库、训练轨迹和比较模式均来自已观察的人类交互；该术语并不意味着生成的对话在整体上像人、自然，或对人有效。

我们采用动作中介视角：语言策略先选择一个离散且人类可读的交互动作，再用语言实现这一选择。这样便将策略来源——人类示范——与任一具体轮次的措辞分离，并形成可追踪的边界，使所选策略及其最终语言实现可以分别评价。从固定轨迹学习还避免了在线探索和人工设计的标量奖励 [li2016deep; schulman2017proximal; ouyang2022training]。

我们以课堂话语研究这一问题。课堂话语包含频繁、细粒度的交互动作。师生交流具有多角色和开放性，结合提问、承接、阐述、质疑、引导与综合，而不是遵循固定任务脚本。真实转录文本还配有成熟的认知动作标注体系，使交互策略可以被可观察地操作化 [songyu79]。我们的目标不是建模教学效果或取代教师，而是研究策略丰富的人类交互如何为语言智能体策略奠定基础。

我们提出 **LA-IQL**，这是一个由领域特定动作空间和人类示范配置的离线框架。LA-IQL 在固定转移上通过以理由为条件的离线逆软 Q 学习来学习动作轨迹 [garg2021iqlearn; wulfmeier2024imitating]，从学习到的策略中选择交互动作，并把语言实现交给角色模型。我们评价：生成轨迹是否复现留出的人类动作模式；这种对齐是否伴随重复行为；理由是否携带与动作有关的信息；以及动作控制权的分配如何影响最终长程对话中可观察的知识引入、关联和发展。

> **图 2：以人为本、动作中介的交互。** LA-IQL 从真实交互轨迹中学习人类可读的策略层，并将策略选择与语言实现分开。该边界既使行为模式可以追踪，也暴露出最终话语未能表达所选动作时的实现风险。

本文贡献有三点：

1. 我们将以人为本的交互策略学习形式化为动作中介的语言策略问题，把人类可读的行为选择与表层实现分开。动作库和训练轨迹都来自真实人类交互，并用留出的真实人类模式评价所得策略。
2. 我们在 LA-IQL 中实例化该形式化：一个以理由为条件的双头语言模型，将词元级监督、动作预测、跨头一致性与固定人类交互轨迹上的离线逆 Q 学习相结合，把 TD 正则化适配到人类可读的交互动作策略。
3. 我们表明，匹配局部人类动作模式不足以证明交互能感知上下文：一阶马尔可夫控制在局部对齐指标上具有竞争力，而学习得到的配置揭示了总体相似度无法呈现的“对齐—重复”权衡，并提供了检查下游知识发展模式的显式边界。

## 2 相关工作

### 2.1 语言中介的智能体交互

端到端对话生成已经从序列到序列模型和分层模型发展到预训练 Transformer [sutskever2014sequence; serban2017hierarchical; vinyals2015neural]。近年来，角色提示和多智能体模拟使用 LLM 生成持续的社会与任务交互 [park2023generative; xi2025rise]。相关教育系统则将监督微调、行为克隆、提示或角色模拟用于师生交流 [zhang2024simulating; tu2023littlemu; chu2025llm]。这些方法可以生成流畅且角色一致的话语，但其交互决策仍是隐式的。因此，表层多样性并不意味着智能体学会了人类轨迹中的结构化策略，而且这些策略难以直接控制或评价 [li2016diversity; zhao2017learning]。

### 2.2 主动性、混合主动权与智能体自主性

HCI 研究认为，对话智能体的能动性不只是回应质量：智能体必须决定何时以及如何参与、承担多少控制权，以及如何让人理解其行为。Liu 等人的 Inner Thoughts 框架将隐性的想法形成与显性的参与分离，以支持主动对话行为 [liu2025proactive]。Cheng 等人表明，对话自主性带来非单调权衡：完全自动化可以改善多任务处理，却可能令人不适；共享控制又可能产生监控成本，抵消性能收益 [cheng2025surrogates]。这些研究促使我们考察对话系统内部如何分配控制，而非假设更强自主性或更多用户/模型监督总是更好。我们的关注点与其互补：研究真实人类交互如何为语言实现之前的动作决策奠定基础，并比较全动作空间策略选择与由提案约束、在策略和角色模型间分配控制的方式。

### 2.3 对话策略学习

任务型对话常被形式化为序列决策：将对话状态映射到抽象动作，并通过强化学习优化策略 [li2016deep; li2018dialogue]。现有方法在状态/动作设计、模拟器构建及在线或离线训练方面各不相同。然而，当策略在抽象对话动作上运作时，表层实现往往依赖模板、检索或独立生成器。对课堂对话生成而言，这种分离会带来问题：动作层面的对齐未必能转化为上下文适当的话语，尤其是在细粒度交互动作空间中。在本文设定下，由两个角色乘以 15 个符号再加 `END` 构成的 31 动作空间，可以与专家轨迹进行显式的动作级比较 [songyu79; amatari2015]。

### 2.4 语言模型的离线模仿

为克服模板式实现和在线探索的局限，近期研究将语言模型与基于价值的方法或离线强化学习结合 [jaques2020way; verma2022chai]。基于模型的方法和在线 RL 依赖准确的用户模拟器或人类交互，在教育中既昂贵又有风险，还可能通过利用模拟器错误而造成语言质量退化 [christiano2017deep; ouyang2022training]。CHAI 使用 Q 函数给语言模型候选项评分，用于任务型回应选择 [verma2022chai]；IQ-Learn 则提供了从专家示范学习的逆 Q 学习目标 [garg2021iqlearn]。Wulfmeier 等人 [wulfmeier2024imitating] 将逆软 Q 学习重写为带 TD 正则化的 MLE 扩展，使离线 IRL 能够用于固定的监督微调数据。

本文与这些方向最接近，但目标问题与建模要求不同。我们不把课堂回应生成当作终点，而把带认知标注的课堂轨迹视为以人为本交互策略的来源；使用其细粒度交互动作空间，让策略决策以生成的理由为条件，并将动作价值估计与话语生成分离。

## 3 方法

LA-IQL 将以人为本、由语言中介的交互视为序列决策：系统在每一步观察对话历史，选择领域定义的交互动作，再以自然语言实现该动作。依照离线逆强化学习的模仿学习视角，LA-IQL 保留监督微调的可扩展性，同时在专家轨迹上加入感知动力学的正则项。本文的技术贡献是集成以理由为条件的策略状态、动作级 TD 正则化模仿、直接策略解码与独立语言实现，而不是提出新的 IRL 目标。我们还考察了受提案约束的候选重排序这一推理时调整。

### 3.1 作为可观察测试平台的课堂交互

我们采用有限上下文马尔可夫决策过程（MDP）近似，从专家课堂示范中进行离线策略学习。令 $H_t$ 表示步骤 $t$ 之前的完整交互历史。策略观测 $s_t=f_{\mathrm{short}}(H_t)$ 由此前两轮对话和两个动作标签组成；它是近似马尔可夫状态，可能遗漏更长程的教学上下文。动作 $a_t$ 是下一待生成的交互动作，来自 31 个离散动作：$\{\text{学生},\text{教师}\}\times 15$ 个认知动作符号（例如基础知识提问/回应、分析与阐述、总结、迁移与创新、回应构建、赞同/质疑/引导），外加终止符 `END`。

被选中的话语 $u_t$ 及其动作将历史推进为 $H_{t+1}=H_t\oplus(a_t,u_t)$，之后 $s_{t+1}=f_{\mathrm{short}}(H_{t+1})$；`END` 标志情节终止。因此，部署时的转移由动作策略和语言实现模型共同诱导，而非仅由 $a_t$ 决定。给定专家示范 $\mathcal D_E=\{(s_t,a_t,s_{t+1})\}$，我们学习动作策略，并根据生成轨迹的多样性及其与专家动作模式的相似度进行评价；语言实现另行评价。这种动作级形式化是 LA-IQL 的第一项设计选择：不直接在开放式词元上应用逆 RL，而是在人类可读的师生认知动作上优化，把表层实现留给独立角色模型。

> **表 1：31 维交互动作空间的紧凑定义。**

我们评价交互模式多样性（G1）、专家模式对齐（G2）、理由—动作一致性（G3），以及生成对话中可观察的知识建构模式（G4）。

### 3.2 数据构建与理由标注

每条专家课堂轨迹被转换为固定窗口转移。在步骤 $t$，$s_t$ 包含最近两句话语和最近两个动作标签，$a_t$ 是已标注的下一交互动作，$s_{t+1}$ 则通过追加下一句话语及 $a_t$ 得到。标注模型依据局部状态和专家下一动作构造理由目标 $z_t^*$，但训练后的模型在推理时不会收到专家动作，而是学习如下分解：

$$
p_\theta(z_t,a_t\mid s_t)=p_\theta(z_t\mid s_t)\,\pi_\theta(a_t\mid s_t,z_t).
$$

训练前，我们过滤掉复制动作符号或规范标签的理由目标，以减少简单标签泄漏。固定监督格式为：

`<Rationale> 解释文本 </Rationale> <Action> 角色-符号标签 </Action>`。

这一构造把每个专家转移变成理由生成和动作级策略学习的配对监督，无需人工指定标量奖励。由于标注模型能看到专家动作，而部署后的理由生成器看不到，训练中混合教师强制理由和模型生成理由，以减小这种分布偏移。

### 3.3 从 MLE 到专家分布匹配

LA-IQL 在以理由为条件的双头模型监督微调基础上加入离线 IQLearn。MLE 在局部预测已观察动作，而 IQLearn 项无需在线交互或学习模拟器，便能把策略与连续状态价值耦合起来。

### 3.4 TD 正则化的离线 IQLearn 目标

我们没有提出新的逆强化学习目标，而采用 Wulfmeier 等人用于语言模仿的离线逆软 Q 学习形式 [wulfmeier2024imitating]。该形式以 IQ-Learn [garg2021iqlearn] 为基础，把逆 RL 重写成带 TD 正则项的最大似然训练。我们的贡献是把它从词元级生成适配到 31 类认知动作策略，其 Q 值由以理由为条件的策略头产生。

定义增广策略状态 $x_t=(s_t,z_t)$。对教师强制样例，$x_t^*=(s_t,z_t^*)$ 与 $x_{t+1}^*=(s_{t+1},z_{t+1}^*)$ 使用相应的已标注理由目标；模型生成理由的样例则以 $p_\theta(z\mid s)$ 的样本替代这些目标。推理时，语言头先根据 $s_t$ 生成 $z_t$，再进行动作评分。

目标包含四项。第一，$\mathcal L_{\mathrm{lm}}$ 是对 LLM 标注的理由和动作前缀计算的语言建模损失。第二，以动作级 MLE 监督策略头：

$$
\mathcal L_{\mathrm{policy}}=-\log\pi_\theta(a_t\mid x_t).
$$

第三，LM 头在理由之后给固定动作标签模板评分，从而产生软动作分布 $p_{\mathrm{LM}}(\cdot\mid x_t)$。使用 KL 一致性损失使策略头与该软分布对齐：

$$
\mathcal L_{\mathrm{consistency}}=D_{\mathrm{KL}}\!\left(p_{\mathrm{LM}}(\cdot\mid x_t)\,\|\,\pi_\theta(\cdot\mid x_t)\right).
$$

最后，策略头为每个 $a\in\mathcal A$ 产生 $Q_\theta(x_t,a)$。相应的软价值和策略为：

$$
\begin{aligned}
V_\theta(x_t)&=\log\!\sum_{a\in\mathcal A}\exp Q_\theta(x_t,a),\\
\log\pi_\theta(a\mid x_t)&=Q_\theta(x_t,a)-V_\theta(x_t).
\end{aligned}
$$

对专家转移 $(x_t,a_t,x_{t+1})$，动作级 TD 残差定义为：

$$
\delta_t:=V_\theta(x_t)+\log\pi_\theta(a_t\mid x_t)-\gamma V_\theta(x_{t+1}).
$$

当 $a_t=\texttt{END}$ 时令 $V_\theta(x_{t+1})=0$。由于 $V_\theta+\log\pi_\theta=Q_\theta$，对应的隐式奖励为 $\delta_t=Q_\theta(x_t,a_t)-\gamma V_\theta(x_{t+1})$。于是 $-\log\pi_\theta(a_t\mid x_t)+\lambda_4\delta_t^2$ 就是 Wulfmeier 等人推导的 MLE 加 TD 目标在动作级的对应形式。总损失为：

$$
\mathcal L=\mathcal L_{\mathrm{lm}}+\lambda_2\mathcal L_{\mathrm{policy}}+\lambda_3\mathcal L_{\mathrm{consistency}}+\lambda_4\delta_t^2.
$$

$\lambda_4$ 在 Stage 1--2 中均为 0；Stage 3 中 Full 将其设为 1，而 no-TD 保持为 0，关闭该项时即退化为无 TD 的目标。实际训练采用连续三个阶段，每个阶段均进行 2 个 epoch 的 LoRA 训练。Stage 1 强调结构合法性，学习率为 $10^{-4}$；Stage 2 从 Stage 1 final 继续联合优化语言与策略目标，学习率为 $10^{-4}$；Stage 3 从 Stage 2 final 继续联合优化，学习率为 $5\times10^{-5}$，并提高自生成理由的比例。三个阶段的教师/自生成理由比例依次为 1.0/0.0、0.8/0.2 和 0.6/0.4。基础模型为 `Qwen/Qwen3.5-0.8B`，最大长度为 512，实际物理 batch size 为 4、梯度累积步数为 1、有效 batch size 为 4，训练 seed 为 42，梯度裁剪为 1.0。Full 与 no-TD 的已恢复配置仅在 Stage 3 的 TD loss 权重上不同，分别为 1 和 0；配置中的“+ TD”旧名称不作为判断依据，以实际 loss 权重为准。

### 3.5 以理由为条件的策略参数化

模型采用显式的、以理由为条件的双头设计，这是 LA-IQL 的第三项设计选择。语言头（因果 LM）先生成 `<Rationale>` 片段，再生成 `<Action>` 标签；策略头对理由片段应用交叉注意力池化，并为每个动作输出一个价值，以参数化 $Q(x,a)$ 与 $\pi(a\mid x)$。决策以生成的理由为条件，而不是依赖单个池化表示。

具体而言，对输入词元序列：语言头产生 $\mathcal L_{\mathrm{lm}}$ 的词元级分数，并通过给 31 个动作的动作前缀模板评分得到软 LM 动作分布；策略头汇聚理由文本，输出用于 $\mathcal L_{\mathrm{policy}}$ 的策略分数；一致性损失 $\mathcal L_{\mathrm{consistency}}$ 则使 LM 动作前缀分布与策略 logits 对齐。

### 3.6 直接策略解码与语言实现

推理时，LA-IQL 将短上下文动作选择与长上下文语言实现分开。令 $h_t=f_{\mathrm{long}}(H_t)$ 表示角色模型看到的较长历史，它不同于策略观测 $s_t$。策略模型先生成 $z_t\sim p_\theta(z\mid s_t)$，再直接选择：

$$
\hat a_t=\arg\max_{a\in\mathcal A}Q_\theta(s_t,z_t,a).
$$

随后，角色模型生成一个实现 $u_t\sim q_\phi(u\mid h_t,\hat a_t)$。因此，LA-IQL 的默认推理规则让学习策略能够访问完整动作空间，而较长上下文只用于实现该策略所选的动作。

> **图 3：LA-IQL 框架与技术流程。** 左侧将以人为本的课堂对话和认知动作标注转换为理由增强的专家转移；中间的理由条件 LA-IQL 通过语言、一致性、策略和 TD 目标联合训练语言组件与策略/价值组件；推理时，学习策略选择交互动作，长上下文角色模型将其实现为话语。右侧还展示候选重排序这一推理时替代方案。

角色模型使用 30 轮对话和 5 个动作步骤的长窗口；需要采样时，温度为 0.8、top-$p$ 为 1。在报告的评价中，生成在遇到 `END` 或达到 100 动作上限时停止。

我们还探索了候选重排序。角色模型按固定格式采样六个动作—话语候选——教师和学生各三个——由此在 $\mathcal C_t=\{(a_{t,j},u_{t,j})\}_{j=1}^6$ 上诱导 $q_\phi(\mathcal C_t\mid h_t)$。选择规则为：

$$
\begin{aligned}
j^*&=\arg\max_{1\le j\le6}Q_\theta(s_t,z_t,a_{t,j}),\\
(a_t,u_t)&=(a_{t,j^*},u_{t,j^*}).
\end{aligned}
$$

该调整通过候选的动作标签评分，而不直接评价 $u_{t,j}$；因此，带有相同动作的候选对 $Q_\theta$ 而言不可区分，平局时保留提案顺序。它受到提案约束，因为未出现在 $\mathcal C_t$ 中的动作无法被选择。与默认直接解码不同，此时由角色模型控制动作支持集，价值模型只在提议动作中选择。

## 4 实验设计

### 4.1 研究问题

- **RQ1：** 动作中介的学习策略与直接语言生成及统计策略相比，在行为多样性、重复性以及与留出的真实人类交互模式对齐方面表现如何？
- **RQ2：** TD 正则化，以及直接全动作空间选择与受提案约束的候选重排序之间的动作控制权分配，如何影响长程行为？
- **RQ3：** 在排除对话状态和显式标签泄漏后，生成理由是否仍包含与动作有关的信息？
- **RQ4：** 学习策略与角色模型之间的动作控制分配，如何影响长程模拟课堂对话中可观察的知识引入、关联和发展模式？

### 4.2 数据与划分

我们使用第 3.1 节所述 K–12 课堂语料中的 50 节课。每轮对话都有说话者角色和一个认知动作符号，由此构造 31 个交互动作及 LA-IQL 所用转移元组。采用冻结、分层的课级划分：35 节训练、5 节验证、10 节测试，而不是按对话轮划分，以避免同一课堂会话的窗口跨分区出现。划分按学科和学段分层，三个分区的课程标识互不重叠；预先冒烟测试所用的两节课不进入测试集。正式评价前记录划分清单、随机种子、数据哈希和各划分的动作频率。相同测试课程曾在较早的 20 步上限运行中被检查过；我们披露这一先前访问，并且不为本文报告的 100 步上限基线评价改变测试成员。

### 4.3 基线与消融

我们将 LA-IQL 与五种已完成的控制方法比较：直接角色条件生成、约束教师提示、专家频率采样、一阶马尔可夫策略和均匀动作采样。后三者隔离分布与序列统计，是诊断性控制，而非学习得到的状态条件策略。马尔可夫控制从冻结训练划分估计 $p(a_t\mid a_{t-1})$，其归纳偏置有意与基于二元组和转移的 G2 指标匹配，但它不观察对话内容、当前教学状态或理由，也不产生可检查实现的话语。它用于检验仅靠总体动作转移能恢复多少局部人类模式对齐。

LA-IQL 使用直接解码规则。我们还报告不重新训练策略、用候选重排序替代直接规则的配置。该比较是在查看当前留出结果后提升为重点，因此属于描述性分析，而非预注册的模型选择主张。两种配置使用相同检查点、课程、上限、角色模型温度和生成种子，但 Direct 的理由温度为 0.0，Candidate 为 0.6；因此我们把它们视为两种系统配置，不把差异完全归因于解码模式。

为了隔离 TD 正则化的训练贡献，我们加入不含 TD 项的匹配消融 **LA-IQL w/o TD (Direct)**。它使用 Direct 解码、0.0 理由温度以及相同的留出课程、角色模型、上下文窗口、生成种子、上限和停止规则。与 Direct–Candidate 对比不同，这是单因素训练消融。

### 4.4 评价指标

交互模式多样性（G1）报告动作 Distinct-2、两步循环率、经验动作熵和专家模式覆盖率，分别刻画局部多样性、重复退化、分布多样性及专家支持的广度。专家模式对齐（G2）采用生成动作模式与专家动作模式之间的 Jensen–Shannon 散度、转移矩阵距离及专家对生成动作 $n$ 元组的支持。这些是行为分布指标，并不能检验某动作对当前对话状态是否合适。马尔可夫控制的归纳偏置与这些指标重叠，因此它是严格的局部模式参照，而不是状态敏感交互能力的证据。

理由—动作一致性（G3）中，我们遮蔽确定性的标签字符串，并在互不重叠的课程上训练独立 31 类动作预测器，输入分别为状态、理由或二者。主要自动指标是增量可模拟性：

$$
\Delta_{\mathrm{sim}}=F_1^{\mathrm{macro}}(s_t,z_t\!\rightarrow\!a_t)-F_1^{\mathrm{macro}}(s_t\!\rightarrow\!a_t),
$$

其中目标是被评价系统选择的动作。我们还报告遮蔽后的仅理由 Macro-F1、多数类、泄漏及打乱理由控制。可预测性本身不能证明因果忠实性 [hase2020leakage; jacovi2020faithfully]，因此把 G3 称为可模拟性诊断，而不是解释质量指标。原计划的动作—话语分类器没有完成训练，其所有结果均暂时排除；目前只保留冗余和规则定义严重违规等次级语言实现检查。修订后的 G4 知识建构操作化正在单独评估，本工作稿暂不报告 G4 的系统级推断。完整协议见附录 B。

### 4.5 评价协议

动作模式 JSD 是主要 G2 自动终点。当前轨迹结果覆盖种子 42–44，每个种子包含 10 节冻结测试课程，生成上限为 100。报告种子聚合值的均值 $\pm$ 总体标准差。对学习系统的比较，还配对 30 个相同“种子—课程”轨迹，以 10 节课程进行聚类重采样，执行 10,000 次 bootstrap 并报告百分位 95% 区间。TD 消融保持 Direct 解码与理由温度固定；Direct 与 Candidate 则理由温度不同，且在观察留出结果后才被考察，因此仅是描述性的系统对比，而非验证性因果解码效应。

> **表 2：G1 核心结果。** 数值为三个种子的均值 $\pm$ 总体标准差。粗体表示每列中表现最好的学习系统；诊断基线可能具有多样性，却不一定与专家对齐。

> **表 3：G2 专家模式对齐。** 数值为三个种子的均值 $\pm$ 总体标准差。粗体表示在所示精度下数值最佳或实质并列的学习系统。一阶马尔可夫控制直接建模若干列所测的局部转移，但不以对话内容为条件。

### 4.6 实验结果

**数据与实现。** 语料含 50 节课、9,419 轮、47 个主题和 9,369 个转移。冻结的课级划分为 35/5/10。每个系统运行三个种子（42–44），每节测试课程最多生成 100 个动作。所有正式轨迹在第 50 个动作之前禁止 `END`。频率与马尔可夫控制只使用冻结训练集。主 Direct 和候选重排序使用 `sft_stage3/final` 检查点；匹配训练消融使用 `sft_stage3_woTD/final`。主 Direct 与 w/o-TD Direct 都使用 0.0 的理由温度，其他生成设置相同；Candidate 使用 0.6，因而不是单因素解码消融。30 条 w/o-TD 轨迹中有 5 条在 57–95 个动作后终止，而主 Direct 的 30 条均达到 100。该停止行为是共享规则下观察到的策略结果，并非事后截断。

**TD 正则化抑制重复的长程组织。** 匹配的 Direct 解码训练消融显示，移除 TD 会将两步循环率从 0.1708 提高到 0.2607（TD 减 w/o-TD 为 $-0.0900$，95% CI $[-0.1400,-0.0438]$，$p=.0002$），将模体复现率从 0.3957 提高到 0.5379（差值 $-0.1422$，95% CI $[-0.2013,-0.0889]$，$p=.0002$），分别相当于移除 TD 后增加 53% 和 36%。使用 TD 时 Distinct-2 和归一化熵在数值上更高，但差异并不可靠。两个系统都覆盖了全部前 20 个专家模式，表明复现率下降并非通过放弃常见模式实现。

局部专家分布对齐变化很小。含 TD 与不含 TD 的配对二元组 JSD 分别为 0.0827 和 0.0835（差值 $-0.0007$，95% CI $[-0.0063,0.0043]$，$p=.8281$），转移 JSD 同样无法区分。二元组支持率显示 TD 有边界性的描述优势（0.9906 对 0.9833，$p=.0510$）。因此，TD 的测得贡献主要集中在抑制复现性序列结构，而非改善局部模仿保真度。

> **表 4：Direct 解码下的匹配 TD 消融。** 在相同 30 个“种子—课程”轨迹上比较含 TD 与不含 TD 的 LA-IQL；按 10 节留出课程聚类，执行 10,000 次 bootstrap。两个系统都使用 0.0 的理由温度及完全相同的其他生成设置。正差值表示 TD 模型数值更大，指标箭头表示期望方向。

**局部转移统计构成强但有限的参照。** 一阶马尔可夫控制取得最低的配对二元组 JSD（0.0791）、汇总二元组 JSD（0.0982）和转移行 JSD（0.1249），Direct LA-IQL 对应为 0.0827、0.1637 和 0.1299。它还达到 0.9935 的二元组支持率，而 Direct LA-IQL 的三元组支持率更高（0.8772 对 0.8557）。

这一结果来自模型与测量的实质匹配：控制方法从训练轨迹估计 $p(a_t\mid a_{t-1})$，二元组 JSD 和转移行 JSD 测量的恰是相应局部动作结构；保守重采样训练支持的转移也会带来高支持率和覆盖率。这些是该方法在既定 G2 终点上的真实优势，不能视为应被忽略的错误。但它从不观察对话文本或教学状态，无法说明局部常见转移在上下文中是否合适。因此，该结果证明简单序列统计可以复现局部人类模式，而不是证明马尔可夫策略是能力更强的交互智能体。这也说明为何必须将 G2 与状态条件决策轨迹、重复诊断及动作—话语实现检查一并报告。

**在学习架构内部，对齐伴随着重复的局部结构。** Direct LA-IQL 比 Candidate 更接近留出专家分布：配对二元组 JSD 为 0.0827 对 0.1059，训练专家二元组支持率为 0.9906 对 0.9522。30 个匹配单元上的 Direct–Candidate JSD 差值为 $-0.0232$，95% CI $[-0.0305,-0.0152]$，BH 校正后 $q<.0001$。

但这种对齐不意味着交互动力学全面更好。Direct 的两步循环更多（0.1708 对 0.0488；差值 $+0.1220$，95% CI $[0.0986,0.1447]$，$q<.0001$），重复模体也更多（0.3957 对 0.1413；差值 $+0.2543$，95% CI $[0.2100,0.2963]$，$q<.0001$）。Distinct-2 没有可靠差异（$-0.0108$，95% CI $[-0.0377,0.0165]$，$q=.5483$）。因此，Candidate 对动作序列组织方式的改变明显大于对局部组合多样性的改变。

> **表 5：Direct–Candidate 配对比较。** 覆盖 30 个匹配“种子—课程”单元（10,000 次 bootstrap、百分位区间、对 11 项比较作 BH 校正）。正值表示 Direct 数值更大，箭头表示期望方向。两种配置的理由温度也不同，因此这是系统级对比，而非单因素解码消融。

**理由携带可预测动作的信息。** 在同一 G3-v2 双 TF–IDF 评价器下，Direct 和 Candidate 遮蔽后的仅理由 Macro-F1 分别为 0.3986 与 0.1013，状态单独输入则分别为 0.0159 与 0.0123。种子级增量可模拟性 $\Delta_{\mathrm{sim}}$ 分别为 0.3885 与 0.0863。匹配的 w/o-TD Direct 消融得到仅理由 F1 0.3682、组合 F1 0.3629、$\Delta_{\mathrm{sim}}=0.3372$、准确率 0.6867，描述性地低于 TD Direct 的 0.3986、0.4044、0.3885 和 0.7097。对原始 Direct 和 Candidate，观察到的种子级增量超过全部 20 个课内/角色打乱控制，30 个“种子—课程”增量也全为正。这说明遮蔽后的理由包含与所选动作相关的信息，但不能证明理由因果地决定策略决策或构成忠实解释；我们也不对描述性的 G3 TD 差异提出推断性效应主张。

**语言实现仍是独立风险。** 原计划的 G4-v1 动作—话语分类器没有完成其训练协议，因此本文暂时排除其初步一致率及所有基于该分类器的系统比较。保留的规则检查显示，Direct、Candidate 和 w/o-TD Direct 均无完全重复，也未触发规则定义的严重违规；Direct 与 Candidate 之间以及两个 Direct checkpoint 之间的局部三元冗余均无可靠差异。这些窄范围检查不能衡量语言实现质量或课堂适切性。

## 5 讨论

### 5.1 以人为本的交互是一种来源，而非保证

结果澄清了语言策略“以人为本”的含义：动作词汇、示范及留出的比较模式均来自真实课堂交互。这一基础让策略拥有比无约束的下一话语生成更有结构的策略库，但并不保证交互整体上像人或在上下文中适当。一阶马尔可夫控制的强局部对齐是最清楚的反例：它不阅读对话，却复现了许多人类支持的转移。因此，人类模式相似度只是在某种指定行为表示上的证据，而不是交互质量的完整度量。

用人类轨迹设计对话智能体时，这一区别十分重要。示范既提供有用规律，也限定系统能学到什么。策略可能保守复现常见模式、放大标注或人群偏差，或把熟悉动作组织成重复轨迹。关于“以人为本”的主张应明确哪些元素获得了人类基础——本文是动作本体、训练转移和评价参照——以及哪些仍未验证，包括体验自然度、教学价值和对长期目标的响应。

### 5.2 TD 正则化主要稳定序列组织

匹配的 Direct 解码消融定位了 TD 正则化最清晰的贡献。移除 TD 后，局部专家模式对齐在统计上不可区分，但两步循环增加 53%，重复模体增加 36%。这表明 TD 目标并非只是扩充动作词汇或改善单步模仿；其可观察贡献在于阻止局部合理的选择累积为重复的长程行为。w/o-TD 策略还有 5/30 条轨迹在达到 100 动作前终止，而 TD 策略没有，为 rollout 稳定性提供了进一步描述性证据。

这对交互设计很重要，因为局部适当性和时间组织可能彼此独立地失败。策略可能反复选择在人类数据中各自常见的动作，却仍让交互显得停滞。动作中介表示使这种失败能以复现决策序列的形式被检查。但现有指标不能证明参与者会认为 TD 轨迹更连贯或有用；这需要对完整交互进行人类评价。

### 5.3 动作中介揭示控制权衡

把策略选择与语言实现分开，会显露端到端文本可能掩盖的权衡。直接选择让学习策略访问完整动作空间，与留出人类动作模式对齐更强，却产生更多短循环和重复模体。候选重排序把动作支持限制在角色模型提案中并降低重复，但使轨迹偏离留出动作分布。由于配置的理由温度也不同，这只是描述性系统对比，而非因果解码消融。尽管如此，它说明更强策略自主性并非总是更好：必须联合考虑策略支持、时间组织和措辞的控制。

显式动作边界提供的是结构可追踪性，而非经验证的面向用户解释。它让设计者比较某状态下所选策略与最终话语，并定位失败源于策略选择还是实现。人是否能有效利用该边界进行监督或干预，仍是需要直接研究的 HCI 问题。

### 5.4 对评价策略丰富语言智能体的启示

本测试平台带来三点启示。第一，动作模式指标应配合归纳偏置与指标相匹配的控制方法，否则局部统计复现可能被误判为上下文敏感能力。第二，评价应把策略选择和语言实现视作相互耦合但不同的对象：若话语没有表达动作，合适的动作标签也无用。第三，长程重复必须和多样性、对齐一起报告，因为策略可能覆盖人类支持的模式，却不断在小子集内循环。这些检查能更细致地研究从人类交互学习的语言智能体，而不会把任一相似度分数等同于类人行为。

## 6 结论

本文提出 LA-IQL：一种从固定示范学习以人为本交互策略的动作中介语言策略。它先把策略表示为以理由为条件、人类可读的动作选择，再由长上下文角色模型实现该选择。一阶马尔可夫控制在若干指标上对留出的局部动作转移匹配得与学习系统相当或更好，说明总体人类模式相似度不足以证明上下文敏感交互。在匹配的 Direct 解码消融中，TD 正则化显著减少短循环和重复模体，却没有可靠改变局部专家分布对齐，其主要测得收益因此位于更长程的行为组织。

在 LA-IQL 内部，直接选择比受提案约束的候选重排序更接近留出的人类动作分布，但也更频繁地重复短循环和模体。这些发现表明，以人为本的策略学习不是类人交互的保证，而是一种让策略学习及其失败显式化的方法。构建策略丰富的语言智能体，需要把人类模式对齐、上下文选择、长程重复和语言实现作为彼此依赖但各不相同的问题来评价；能否迁移到本测试平台之外并产生面向人的效用，仍有待证明。

## 局限性

LA-IQL 依赖源自一种课堂标注方案的固定交互动作分类。31 动作空间可能遗漏有教学意义的行为，也可能无法原样迁移到不同学科、年级、语言、文化或课堂形式。尽管框架围绕领域特定状态与动作空间设计，并由更广泛的交互学习问题驱动，本文只评价了课堂对话实例；向对话、多智能体和具身系统的迁移仍是假设，需要领域特定示范与直接实证。方法从离线专家示范学习，因此会复现源语料的标注错误和系统偏差，也无法发现语料中不存在的有效策略。有限上下文策略观测可能遗漏长程教学目标，尽管角色模型能接收更长对话历史。

已完成的 Direct 与 Candidate 配置除动作选择机制外，理由采样温度也不同。因此，其比较只识别可复现的系统级权衡，不能隔离解码模式的因果效应；作出这一归因前需要匹配温度的比较。

生成理由不应被解释为模型内部计算的忠实因果说明。它们是受监督的文本中间变量，可能看似合理却不完整，或与所选动作不一致。受提案约束的调整受候选覆盖限制：再强的 Q 函数也无法选择提案集中不存在的动作，也不能区分带有相同动作标签的话语。直接解码避开支持集限制，却把所选动作的实现交给独立角色模型，后者可能无法忠实表达动作。

最后，自动动作分布指标不能证明教育效果。有关学习成效或教师培训价值的主张，需要教师和学习者在真实环境中的评价。当前 G2 终点还偏重局部动作 $n$ 元组和转移分布；一阶马尔可夫策略可能仅通过保守复用训练支持的转移取得高分，而不响应当前课堂状态的语义内容。要证明上下文决策质量，需要直接的状态匹配判断或干预。

## 伦理考虑

课堂对话可能包含个人信息并涉及未成年人，因此隐私、同意和数据治理是本研究的核心。任何课堂转录文本的使用都应遵循源数据集的同意条件、许可与访问限制，并在模型训练或发布前移除身份信息。生成对话不得被宣传为合格教师的替代品，也不得在缺少人类监督时用于高风险评价。

系统可能复现示范和底层语言模型中的文化、语言或教学偏差。尤其是，在狭窄课堂人群上训练的策略可能错误地把一种话语风格视为普遍的专家风格。因此，部署应包括子群体与跨情境评价、教育工作者审查、机器生成结果的清晰披露，以及让用户质疑或覆盖生成建议的机制。LLM 生成的理由标注也需要质量审计，因为流畅解释可能掩盖标注错误或无依据假设。

## 参考文献

参考文献的作者、题名及出版信息不作翻译，沿用源文件 [`references.bib`](references.bib) 中的原文书目。

## 附录 A：补充实验结果

> **表 A1：冻结课级划分的语料统计。** 各行主题数为去重计数，不一定相加等于语料总数，因为有一个主题名称出现在多个分区中。

**训练与推理配置。** LA-IQL 直接选择价值最高的动作，再取得一个角色模型实现。候选重排序则生成六个动作—话语提案，并把策略限制在该集合所含动作上。这改变了策略与角色模型间的控制分配，但不重新训练策略。已完成配置的理由温度也不同（0.0 对 0.6），所以下述比较是描述性系统对比，而非隔离的解码消融。w/o-TD Direct 条件仅从训练中移除 TD 项，保留主 Direct 的生成配置，包括 0.0 理由温度。其 30 条轨迹有 5 条在允许的最小上限之后终止，而主 Direct 没有。

**主模型三阶段训练配置。** 三个阶段均训练 2 个 epoch。Stage 1/2/3 的学习率依次为 $10^{-4}$、$10^{-4}$ 和 $5\times10^{-5}$；policy loss 权重依次为 0.25、1.0、1.0；consistency loss 权重为 0、0.1、0.1；LM action-class loss 权重为 1.0、0.5、0.5。Full/no-TD 的 TD loss 权重在 Stage 1 和 Stage 2 均为 0，在 Stage 3 分别为 1/0。recovery loss 权重依次为 0.5、0.5、0.75。实际运行采用物理 batch 4、梯度累积 1，而非配置注释中已经过时的 micro-batch 1、累积 4。尚未确认的 optimizer、scheduler/warm-up、LoRA 细节、数值精度、精确 step 数、模型与 tokenizer revision、checkpoint selection rule 和历史代码版本不由当前默认值补写。

> **表 A2：相同留出课程上的训练与推理比较。** JSD 是 G2 配对课级二元组散度；未完成训练的 G4-v1 分类器结果已暂时移除。

图 A1 与 A2 从互补角度汇总这些比较：前者强调配对不确定性，后者保留报告的指标尺度和种子级变化。

> **图 A1：TD 训练和解码配置在统一期望方向后的配对效应。** 对结果表中标记 $\downarrow$ 的指标，将差值和区间乘以 $-1$，使正值始终有利于首先命名的系统（TD 或 Direct）。水平线为按 10 节留出课程聚类、执行 10,000 次配对 bootstrap 后的百分位 95% 区间。实心标记表示报告的 $p<.05$ 或 BH 校正 $q<.05$，空心标记表示不显著或未检验。由于两种配置的理由温度也不同，Direct–Candidate 面板是描述性系统对比。

> **图 A2：三个学习系统配置的 G1 与 G2 核心结果。** 点表示均值，误差线表示种子 42–44 的总体标准差，每个种子有 10 节留出课程。连接线仅为视觉引导，不代表时间轨迹；箭头表示期望的指标方向。

**G3-v2 理由—动作可模拟性。** 独立预测器遮蔽动作符号、规范名称、缩写及确定性标签字符串。状态与理由采用独立的字符 2–5 元 TF–IDF 表示和经验证集选择的线性分类器。三个系统使用相同的冻结评价器协议。

> **表 A3：G3-v2 理由—动作可模拟性。** 数值为三个种子级分数的均值。目标是各系统所选动作；该结果是关联诊断，而非因果忠实性度量。

**判断状态。** 已生成盲测条目、随机化清单及评分量规，但尚无完成的人类判断分数。未完成训练的 G4-v1 分类器结果已被排除，仅保留独立的规则诊断。我们不依据严重规则违规率为零而对事实性、安全性、教学适切性、体验自然度或学习成果作出主张。

## 附录 B：评价细节

完整 G1 套件还包括 Distinct-1/3、重复局部模体和跨轨迹方差。G3 在预测前遮蔽动作符号、规范名称、缩写和确定性标签字符串，并报告泄漏率、逐类分数、混淆矩阵、多数类和打乱理由控制。

当前 G3 评价通过独立预测和课内/角色打乱控制来建立关联。删除、释义和角色兼容反事实干预 [deyoung2020eraser] 仍是未来的因果敏感性检验。已经准备好盲化动作支持和课堂适切性材料，但尚未报告完成的人类判断分数，因此我们避免对解释忠实性、教育效果或体验自然度作出主张。

## 附录 C：完整 G1 统计

> **表 C1：全部已完成系统的完整 G1 统计。** 数值为种子 42、43、44 的均值 $\pm$ 总体标准差；每个种子包含 10 节留出课程。
